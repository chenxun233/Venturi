{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to Venturi","text":""},{"location":"#introduction","title":"Introduction","text":"<p>Venturi is an FPGA plus Linux project centered on receiving Nasdaq ITCH 5.0 market data, building order-book state in hardware, and exporting FPGA-generated events to host memory through PCIe DMA.</p> <p>The documentation below reflects the current FPGA-side implementation in this repository. The active receive path is already documented module by module, from Ethernet ingress to host-memory writes.</p>"},{"location":"#current-fpga-side","title":"Current FPGA Side","text":"<p>The present FPGA design is split into two main clock domains:</p> <ol> <li>Receive path at 156.25 MHz: Ethernet data is converted to XGMII, framed into an AXI-style stream, parsed as ITCH messages, and reduced into order-book events.</li> <li>PCIe path at 250 MHz: those events cross into the PCIe user clock domain, where software-programmed RX queues are serviced by a DMA write engine.</li> </ol> <p>At a high level, the pipeline is:</p> <p><code>pcs_pma_wrapper -&gt; MAC_layer_rx -&gt; order_book_parser -&gt; order_book_builder -&gt; timestamper -&gt; async_fifo -&gt; rx_dma_stage -&gt; pcie_wrapper</code></p>"},{"location":"#fpga-data-path","title":"FPGA Data Path","text":"<ul> <li>FPGA overview: top-level summary of the active FPGA hierarchy.</li> <li>pcs_pma_wrapper: converts the Ethernet PHY-side interface into the XGMII stream used by the receive logic.</li> <li>MAC_layer_rx: strips preamble/SFD and converts XGMII RX traffic into a simpler streaming interface.</li> <li>order_book_parser: walks the Ethernet/IP/UDP headers and decodes supported ITCH 5.0 message types.</li> <li>order_book_builder: updates per-symbol order-book state and produces event payloads for downstream export.</li> <li>timestamper: provides the event timestamp carried with parsed data.</li> <li>async_fifo: crosses event payloads from the receive clock domain into the PCIe user clock domain.</li> <li>rx_dma_config: exposes the RX queue programming and status registers to host software over MMIO.</li> <li>rx_dma_stage: selects a non-empty event queue, checks host ring availability, and issues fixed-size PCIe DMA writes.</li> <li>pcie_wrapper: connects the user logic to the Xilinx PCIe Gen3 IP and separates MMIO traffic from requester traffic.</li> </ul>"},{"location":"#current-focus-areas","title":"Current Focus Areas","text":"<p>The documented FPGA-side behavior today is mainly the RX and host-export path:</p> <ul> <li>Ethernet receive and frame adaptation</li> <li>ITCH message parsing</li> <li>order-book state update</li> <li>clock-domain crossing into PCIe logic</li> <li>MMIO-based queue programming</li> <li>fixed-size RX DMA writes into host memory</li> </ul> <p>This means the existing docs are strongest on the path from incoming wire data to host-visible DMA records.</p>"},{"location":"#documentation-map","title":"Documentation Map","text":"<ul> <li>FPGA pages: start from FPGA overview for the hardware hierarchy.</li> <li>DMA and queueing: use rx_dma_config, rx_dma_stage, and arbiter.</li> <li>PCIe internals: use pcie_wrapper overview, then drill into CQ_parser, CC_formatter, RQ_formatter, and RC_parser.</li> </ul>"},{"location":"FPGA/MAC_layer_rx/","title":"MAC_layer_rx","text":"<p><code>MAC_layer_rx</code> converts a 64-bit XGMII receive stream into a simplified AXI-Stream style interface for the parser pipeline. It also does the reversion from little-endian to big-endian. The implementation lives in <code>verilog_src/MAC_layer/MAC_layer_rx.v</code>.</p> <p>It also strip the preamble and SFD at the start of each frame.</p> <p>This is a module you can find in literally every NIC-related FPGA logic.</p>"},{"location":"FPGA/MAC_layer_rx/#design-logic","title":"Design logic","text":"<p>From XGMII to MAC layer, the number of cases is limited, and the data is streaming. Thus, we can prepare data for all the possible situations and treat the situations as a switch, output the data when the corresponding situation is true.</p> <p>There can be three states: <code>IDLE</code>, <code>SOF</code> and <code>TERM</code>. <code>SOF</code> means not only the start of the frame, but also the receiving process. In each state, different location of <code>sof</code> and <code>eof</code> can end up with different outputs, these are what we are going to prepare.</p> <p>Combinational logic is widely used for the data preparing.</p>"},{"location":"FPGA/MAC_layer_rx/#interface","title":"Interface","text":""},{"location":"FPGA/MAC_layer_rx/#inputs","title":"Inputs","text":"<ul> <li><code>i_xgmii_rx_clk</code> is the receive clock for the whole module.</li> <li><code>i_xgmii_rx_rst</code> is an active-high reset that clears the current frame state.</li> <li><code>i_xgmii_rxd</code> and <code>i_xgmii_rxc</code> carry one 64-bit XGMII beat plus 8 control bits.</li> <li><code>i_rx_status</code> is treated as link health; if it drops, the module flushes its internal state.</li> <li><code>i_axi_rx_ready</code> is not true backpressure. If the downstream is not ready, the current frame is dropped because XGMII itself cannot stall.</li> </ul>"},{"location":"FPGA/MAC_layer_rx/#outputs","title":"Outputs","text":"<ul> <li><code>o_axi_rx_data</code>, <code>o_axi_rx_valid</code>, <code>o_axi_rx_keep</code>, and <code>o_axi_rx_last</code> form the downstream receive stream.</li> <li><code>o_frame_start</code> pulses when a new frame is accepted (not used now);</li> </ul>"},{"location":"FPGA/MAC_layer_rx/#start-and-end-detection","title":"Start and End Detection","text":"<ol> <li>Start indication can only be at 0th or 4th place (from left) in <code>i_xgmii_rxc</code>.</li> <li>End indication can be any bit in <code>i_xgmii_rxc</code>.</li> <li>They are using combinational logic, so once there is valid bit in <code>i_xgmii_rxc</code>, the corresponding sof or eof bit will be asserted at the same time.</li> </ol> <pre><code>    genvar      idx;\n    generate\n        for (idx = 0; idx &lt; 2; idx = idx + 1 ) begin:sof_check\n            assign sof_location [idx] = i_xgmii_rxc[idx*4] &amp;&amp; (i_xgmii_rxd[idx*32 +:8] == XGMII_START);\n        end\n    endgenerate\n    generate\n        for (idx = 0; idx &lt; 8; idx = idx + 1 ) begin:eof_check\n            assign eof_location [idx] = i_xgmii_rxc[idx] &amp;&amp; (i_xgmii_rxd[idx*8 +:8] == XGMII_TERM);\n        end\n    endgenerate\n</code></pre> <p><code>sof_location[0]</code> means the frame starts in byte 0 of the current word. <code>sof_location[1]</code> means the frame starts in byte 4, so the payload must be stitched across two cycles. Below is the illustration of possible sof and eof location in xgmii_rxc.</p> <p></p>"},{"location":"FPGA/MAC_layer_rx/#state-machine","title":"State Machine","text":"<p>There are three states in <code>cur_state</code></p> <ul> <li><code>IDLE</code>: <ol> <li>waiting for start of the frame. Once any of <code>sof_location</code> bit is true (|sof_location), it changes to <code>SOF</code> state.</li> <li>Assert <code>o_frame_start</code>. This is used to start <code>time_stamp</code>.</li> </ol> </li> <li> <p><code>SOF</code>:</p> <ol> <li>It means the start of the frame, as well as frame transmitting.</li> <li><code>valid_d1</code> will be asserted, which is the indication of the payload. As it is a register, there is one cycle  latency for it to be true asserted. This is done on purpose which is used to discard the preamble and SFD .</li> <li>It checks <code>|eof_location</code>, which is an indication of end of the frame.</li> <li>It assign <code>eof_location</code> to <code>eof_reg</code>. This is used for the proper <code>last</code> and <code>keep</code> signal in AXI channel.</li> </ol> </li> <li> <p><code>TERM</code>: </p> <ol> <li>desert <code>valid_1</code>.</li> <li>return to <code>IDLE</code>.</li> </ol> </li> </ul>"},{"location":"FPGA/MAC_layer_rx/#o_axi_rx_valid-signal","title":"<code>o_axi_rx_valid</code> signal","text":"<ol> <li> <p>Assertion</p> <p>When <code>cur_state</code> is <code>SOF</code>. directly assign <code>valid_d1</code> to it. </p> </li> <li> <p>De-assertion</p> <p>It depends on sof and eof. Even when <code>cur_state</code> is <code>EOF</code>, it does not mean we can de-assert it. Some combination of sof and eof can make <code>o_axi_rx_valid</code> last for one more cycle.</p> </li> </ol>"},{"location":"FPGA/MAC_layer_rx/#data-parsing","title":"Data Parsing","text":"<ol> <li> <p>if <code>sof_reg</code> is <code>2'b00</code></p> <p>It means no data.</p> </li> <li> <p>if <code>sof_reg</code> is <code>2'b01</code></p> <p>It means the data is always <code>i_xgmii_rxd_1</code></p> </li> <li> <p>if <code>sof_reg</code> is <code>2'b10</code></p> <p>It means the when we detecting the sof, half of the data is already there. Thus, we should make a stitch like <code>{current_rxd,previous_rxd}</code>, where previous_rxd is <code>data_saver</code>. Below shows how data is stitched.</p> </li> </ol> <p></p>"},{"location":"FPGA/MAC_layer_rx/#o_axi_rx_last-signal","title":"<code>o_axi_rx_last</code> signal","text":"<p>It depends on <code>cur_state</code>, <code>sof_reg</code> and <code>eof_reg</code>. To be honest, it should not be asserted in the first few cycles in practice, as there is always a minimal length for an Ethernet frame. I made the assertion of it possible in <code>SOF</code> just to align with the algorithm. Just keep in mind that depending on combination of <code>sof_reg</code> and <code>eof_reg</code>, the last signal can be asserted at <code>TERM</code> or before <code>TERM</code>, as there is nothing left for transmit.</p>"},{"location":"FPGA/MAC_layer_rx/#o_axi_rx_keep-signal","title":"<code>o_axi_rx_keep</code> signal","text":"<p>Here we used enumination, to list all the possible combination. A simple math can manage.</p>"},{"location":"FPGA/MAC_layer_rx/#design-limits","title":"Design Limits","text":"<ul> <li>No CRC check: bad FCS is not detected here.</li> <li>CRC is preserved: downstream logic must ignore or strip it if needed.</li> <li>No buffering on stall: <code>!i_axi_rx_ready</code> resets the receive state and abandons the current frame.</li> <li>64-bit specific: the logic assumes <code>DATA_WIDTH=64</code> and <code>CTRL_WIDTH=8</code>, even though they are written as parameters.</li> </ul>"},{"location":"FPGA/async_fifo/","title":"async_fifo","text":"<p>This module is an asynchronous FIFO used to pass event payloads between two clock domains. In this project, it transfers <code>order_book_builder</code> events from the receive clock domain (xgmii clock domain, which is 156.25 MHz) into the PCIe/control-plane clock domain (250 MHz).</p>"},{"location":"FPGA/async_fifo/#introduction","title":"Introduction","text":"<p>In <code>verilog_src/Top.v</code>, it is instantiated as <code>event_cdc_fifo_inst</code>.</p> <p>Its write side (156.25 MHz) runs on <code>i_wr_clk</code>, and its read side (250 MHz) runs on <code>i_rd_clk</code>. </p>"},{"location":"FPGA/async_fifo/#design-logic","title":"Design Logic","text":"<p>The FIFO uses a shared memory array: data is written in the <code>156.25 MHz</code> clock domain and read in the <code>250 MHz</code> clock domain. The main challenge is not the memory itself, but the control logic required to determine when the FIFO is full on the write side and empty on the read side. That requires safe clock-domain crossing and Gray-coded pointer transfer.</p> <p>The diagram below shows the internal structure of this <code>async_fifo</code>:</p> <p></p> <p>At the center of the design is a memory array accessed from two clock domains. Solid blocks represent registers, while dashed blocks represent combinational signals.</p> <p>On the write side, running at <code>156.25 MHz</code>, the control logic must determine whether the FIFO is full so that further writes can be blocked. This <code>full</code> decision depends on the read pointer, which is generated in the read clock domain at <code>250 MHz</code>. To transfer that information safely across the clock-domain boundary, the read pointer is first encoded in Gray code, then passed into the write domain through a two-stage synchronizer. After synchronization, the write-side logic compares the next write pointer against the synchronized read pointer to determine whether the FIFO is full.</p> <p>Similarly, on the read side, the write pointer is converted to Gray code before being transferred into the read clock domain and synchronized there for empty detection.</p> <p>Gray code is used because only one bit changes between adjacent pointer values. This reduces the risk of sampling multiple bit transitions during clock-domain crossing and therefore makes the pointer transfer more robust.</p>"},{"location":"FPGA/async_fifo/#interface","title":"Interface","text":""},{"location":"FPGA/async_fifo/#inputs","title":"Inputs","text":"<ul> <li><code>i_wr_clk</code>: write clock.</li> <li><code>i_wr_rst</code>: write-domain reset.</li> <li><code>i_wr_en</code>: write request.</li> <li><code>i_wr_data</code>: write payload.</li> <li><code>i_rd_clk</code>: read clock.</li> <li><code>i_rd_rst</code>: read-domain reset.</li> <li><code>i_rd_en</code>: read request.</li> </ul>"},{"location":"FPGA/async_fifo/#outputs","title":"Outputs","text":"<ul> <li><code>o_wr_full</code>: write-side full flag.</li> <li><code>o_rd_empty</code>: read-side empty flag.</li> <li><code>o_rd_valid</code>: read-data valid pulse.</li> <li><code>o_rd_data</code>: read payload.</li> </ul>"},{"location":"FPGA/timestamper/","title":"timestamper","text":"<p><code>timestamper</code> is the local timestamp source used by the exported order-book event payload.</p>"},{"location":"FPGA/timestamper/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/timestamper.v</code>.</p>"},{"location":"FPGA/timestamper/#design-logic","title":"Design Logic","text":"<p>The module increments a free-running <code>tick_counter</code> on every clock cycle.</p> <p>When <code>i_event</code> is high, it copies the current counter value into <code>o_event_timestamp</code>.</p> <p>In the current symbol_book integration, <code>i_event</code> is tied high, so the module effectively publishes the current cycle counter every cycle. The event path then captures the timestamp that is present when the symbol snapshot is emitted.</p>"},{"location":"FPGA/timestamper/#interface","title":"Interface","text":""},{"location":"FPGA/timestamper/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code></li> <li><code>i_rst</code></li> <li><code>i_event</code></li> </ul>"},{"location":"FPGA/timestamper/#outputs","title":"Outputs","text":"<ul> <li><code>o_event_timestamp</code></li> </ul>"},{"location":"FPGA/order_book_parser/","title":"order_book_parser","text":"<p>Parse order messages packed in ITCH 5.0 in the payload from MAC_layer_rx. It also drops unrelated messages.</p>"},{"location":"FPGA/order_book_parser/#design-logic","title":"Design Logic","text":"<p>The module first walks through fixed packet headers, then peeks message count and message type, and finally extracts fields such as <code>stock_locate</code>, <code>order_ref_num</code>, <code>shares</code>, and <code>price</code>, as parallel signals to the next module.</p>"},{"location":"FPGA/order_book_parser/#the-header","title":"The header","text":"<p>Before pasing the messages, we have to deal with Ethernet head, IP header and UDP/TCP header (The preabmle+SFD has already been filtered out in MAC layer), as illustrated below:</p> <p></p> <p>The lengths of headers are always fixed. The parsing of them is easy. We use a counter (<code>head_counter</code>) to indicate the end of headers.</p>"},{"location":"FPGA/order_book_parser/#the-messages","title":"The messages","text":"<p>For each message, parsing is not finised until we hit the end from <code>i_axi_rx_data</code>. Thus, we have to wait for it (use a buffer). However, The end of the previous message and the start of the next message may be mixed in one <code>i_axi_rx_data</code>. So, we need another counter (<code>buffed_bytes</code>).</p>"},{"location":"FPGA/order_book_parser/#states","title":"States","text":"<p>In the big picture, as stated above, the parsing can be divided into two phases:</p> <ol> <li>header parsing.</li> <li>message parsing.</li> </ol> <p>header parsing triggers message parsing. The flow chart is shown below:</p> <p></p> <ol> <li>At the begining, header parsing will be kicked off when <code>i_axi_rx_valid == 1</code>, from which <code>head_counter</code> starts to <code>+1</code>. </li> <li>When it reaches <code>6</code>, it triggers the message parsing phase, It gets <code>msg_count</code> and start to buffer. </li> <li>Every time when <code>msg_count !=1</code>, it peeks the type and wait until the buffer has enough bytes. </li> <li>Once it parsed the message, <code>msg_count &lt;= msg_count -1;</code> and <code>buffered_bytes</code> will also be subtracted corresponding length.  </li> <li>The peak type -&gt; data to buffer -&gt; parse loop will be exectued again, until <code>msg_count == 0</code>. </li> <li>For unknown types, the <code>default</code> case will do nothing except subtract <code>msg_count</code> and <code>buffered_bytes</code>.</li> </ol>"},{"location":"FPGA/order_book_parser/#control-from-the-host","title":"Control from the host","text":"<p>I want to add control like filtering out frames sent to other IP or MACs, realized by host sending register control signals through PCIe. However, this has nothing to do with the parsing itself, so now it is suspended. Will be realized in the future.</p>"},{"location":"FPGA/order_book_parser/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_parser/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code> is the parser clock.</li> <li><code>i_rst</code> clears the header parser, message parser, and output registers.</li> <li><code>i_axi_rx_data</code>, <code>i_axi_rx_valid</code>, <code>i_axi_rx_keep</code>, <code>i_axi_rx_last</code>, and <code>i_axi_rx_ingress_tick</code> come from <code>MAC_layer_rx</code>.</li> <li><code>i_ctl_dst_mac</code>, <code>i_ctl_dst_ip</code>, <code>i_ctl_dst_port</code>, <code>i_promiscuous</code>, <code>i_sync_fire</code>, and <code>i_ctl_reg</code> program the parser settings registers.</li> </ul>"},{"location":"FPGA/order_book_parser/#outputs","title":"Outputs","text":"<ul> <li><code>o_axi_rx_ready</code> is tied high, so this block always accepts incoming beats.</li> <li><code>o_msg_valid</code> pulses when one complete message has been decoded.</li> <li><code>o_seq_num</code> and <code>o_rx_ingress_tick</code> carry packet-level context.</li> <li><code>o_msg_type</code>, <code>o_stock_locate</code>, <code>o_order_ref_num</code>, <code>o_new_order_ref_num</code>, <code>o_buy_sell</code>, <code>o_shares</code>, <code>o_price</code>, and <code>o_timestamp</code> carry the parsed order-book fields.</li> </ul>"},{"location":"FPGA/order_book_parser/#sliding-buffer","title":"Sliding Buffer","text":"<p>The module keeps a 512-bit (64 bytes) history window so fields can cross AXI beat boundaries without special cases.</p> <pre><code>reg  [511:0] prev_buff;\nwire [511:0] cur_buff = {prev_buff[447:0], i_axi_rx_data};\nwire [6:0] valid_bytes = buffed_bytes &lt; 7'd6 ? 7'd0 : buffed_bytes - 7'd6;\n</code></pre> <p><code>prev_buff</code> is sequential state. <code>cur_buff</code> mixes the previous window with the current beat, which lets the parser read the next field one cycle earlier. <code>valid_bytes</code> accounts for the fixed 6-byte gap between the current parse boundary and the start of the valid window.</p> <p>here the <code>-7'd6</code> is because the time we parse the message body, we are actually at the half of <code>seq_num</code> (<code>header_counter</code> is <code>7</code>), which is 3 bytes. Plus the <code>message_count</code> field, we have total <code>6 bytes</code> offset from the buffer starting point.</p>"},{"location":"FPGA/order_book_parser/#state-machine","title":"State Machine","text":"<p>The body parser is a 5-state machine.</p> <ul> <li> <p><code>IDLE</code>:</p> <ol> <li>Clear parser-local state such as <code>prev_buff</code>, <code>o_msg_valid</code>, <code>buffed_bytes</code>, <code>o_msg_type</code>, and <code>msg_count</code>.</li> <li>Wait until <code>head_counter == 6</code>, then move to <code>PEEKING_MSG_COUNT</code>.</li> </ol> </li> <li> <p><code>PEEKING_MSG_COUNT</code>:</p> <ol> <li>Wait for <code>i_axi_rx_valid</code>.</li> <li>Latch the packet message count from <code>i_axi_rx_data[31:16]</code>.</li> <li>Shift the current AXI beat into <code>prev_buff</code>, increment <code>buffed_bytes</code>, and move to <code>PEEKING_TYPE</code>.</li> </ol> </li> <li> <p><code>PEEKING_TYPE</code>:</p> <ol> <li>Clear the previous decoded message outputs.</li> <li>If <code>msg_count == 0</code>, return to <code>IDLE</code>.</li> <li>Otherwise, wait for <code>i_axi_rx_valid</code>, peek the next message type with <code>take_data(cur_buff, valid_bytes-2, 1)</code>, shift in the new beat, and move to <code>PARSING_BODY</code>.</li> </ol> </li> <li> <p><code>PARSING_BODY</code>:</p> <ol> <li>If <code>buffed_bytes &gt;= 64</code>, enter <code>ERROR</code>.</li> <li>If <code>valid_bytes</code> is large enough for <code>msg_len_bytes(peeked_type)</code>, decode one full message, decrement <code>msg_count</code>, and return to <code>PEEKING_TYPE</code>.</li> <li>Otherwise, keep shifting AXI beats into the sliding buffer until enough bytes are available.</li> </ol> </li> <li> <p><code>ERROR</code>:</p> <ol> <li>Stop decoding and hold state until <code>i_axi_rx_last</code> arrives.</li> <li>Return to <code>IDLE</code> at the end of the packet.</li> </ol> </li> <li> <p><code>PARSING_HEADER</code>:</p> <ol> <li>This state is declared in the RTL but is not used by the current transition logic.</li> </ol> </li> </ul>"},{"location":"FPGA/order_book_parser/#supported-message-types","title":"Supported Message Types","text":"<p>The parser recognizes seven message types and uses <code>msg_len_bytes()</code> to decide when a full message is present.</p> <ul> <li> <p><code>A</code> (<code>7'd38</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, <code>buy_sell</code>, <code>shares</code>, and <code>price</code>.</li> <li>Skips tracking number and stock symbol.</li> </ol> </li> <li> <p><code>X</code> (<code>7'd25</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, and canceled <code>shares</code>.</li> </ol> </li> <li> <p><code>D</code> (<code>7'd21</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, and <code>order_ref_num</code>.</li> </ol> </li> <li> <p><code>U</code> (<code>7'd37</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, original <code>order_ref_num</code>, <code>new_order_ref_num</code>, <code>shares</code>, and <code>price</code>.</li> </ol> </li> <li> <p><code>E</code> (<code>7'd33</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, and executed <code>shares</code>.</li> <li>Skips match number.</li> </ol> </li> <li> <p><code>F</code> (<code>7'd42</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, <code>buy_sell</code>, <code>shares</code>, and <code>price</code>.</li> <li>Skips stock symbol and attribution.</li> </ol> </li> <li> <p><code>C</code> (<code>7'd38</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, <code>shares</code>, and <code>price</code>.</li> <li>Skips match number, printable flag, and attribution.</li> </ol> </li> <li> <p>Shared note:</p> <ol> <li>Fields such as tracking number are consumed for alignment but are not exposed at the output interface.</li> </ol> </li> </ul>"},{"location":"FPGA/order_book_parser/#field-extraction","title":"Field Extraction","text":"<p><code>take_data()</code> reads a field from <code>cur_buff</code> by byte index and width, then preserves the original byte order in the output register.</p> <pre><code>take_data[(width_bytes-i)*8-1 -: 8] =\n    prev_buff[(index_bytes-i)*8-1 -: 8];\n</code></pre> <p>This helper is the reason each message case can describe fields in byte offsets instead of manually slicing every possible boundary crossing.</p>"},{"location":"FPGA/order_book_parser/#waveform-snapshot","title":"Waveform snapshot","text":"<p>Below shows an example of the waveform, in which the orders are parsed by this module. The pink waves are axi data comming from MAC_layer_rx. It can be noted after several cycles (the headers), the messages are parsed out, indicated by <code>o_msg_valid</code>. There are also three examples of message <code>TYPE_A</code>, <code>TYPE_F</code> and <code>TYPE_U</code>.</p> <p></p>"},{"location":"FPGA/order_book_parser/#design-limits","title":"Design Limits","text":"<ul> <li><code>o_axi_rx_ready</code> is always <code>1'b1</code>, so this module provides no receive backpressure.</li> <li><code>i_axi_rx_keep</code> is present on the interface but is not used in the current parser logic.</li> <li>The programmable destination filters are stored but not applied to gate parsing.</li> <li>If <code>buffed_bytes &gt;= 64</code>, the parser enters <code>ERROR</code> and waits for <code>i_axi_rx_last</code>.</li> </ul>"},{"location":"FPGA/overview/","title":"FPGA Overview","text":"<p>This section documents the active FPGA-side data path in Venturi.</p>"},{"location":"FPGA/overview/#introduction","title":"Introduction","text":"<p>It performs the parsing and building of order books from the incoming UDP frames, where NASDAQ ITCH 5.0 protocol is used. It outputs the best ask/bid prices as well as the corresponding shares per symbol.</p> <p>Below gives the hierarchy of the FPGA side design:</p> <p></p> <p>The setup is also given here: </p> <p>The jtag wires were soldered to an adapter to the golden fingers in Exanic x10, which does not have a standard jtag interface. The soldering procedure can be found in this link: Connect gold fingers to JTAG.</p>"},{"location":"FPGA/overview/#main-modules","title":"Main Modules","text":"<ul> <li>PCS_PMA_wrapper: converts the UDP frames to xgmii interface. (this one I copied from the official design example.)</li> <li>MAC_layer_rx: converts XGMII RX data into a simpler streaming interface.</li> <li>order_book_parser: decodes ITCH messages from the received payload stream.</li> <li>order_book_builder: tracks top-of-book state per symbol.</li> <li>async_fifo: crosses event payloads from the RX clock into the PCIe clock.</li> <li>timestamper: generates the exported event timestamp (Actually there should be one stamp at the mac layer).</li> <li>rx_dma_config: MMIO register block for queue programming.</li> <li>rx_dma_stage: DMA write engine for fixed-size host records.</li> <li>pcie_wrapper: PCIe integration shell and AXI-Stream channel wiring.</li> </ul>"},{"location":"FPGA/pcs_pma_wrapper/","title":"pcs_pma_wrapper","text":"<ol> <li>It can be replaced by a well-designed pcs_pma IP core. However, this does not work in my board. So, I just copied the gtwizard IP core and the related official design example to replace it.</li> <li>One thing needs to mind is that the tx polarity is asserted while the rx is not, which means the tx differential lines are swapped in this board.</li> <li>One thing I can share: The mapping from xgmii to 64b66b, as well as the 64b66b frame structure. You can also use it vice versa. I provide them below:</li> <li>The mapping covers all the possiblities, you can use combinational logic to do the decode (encode). I may expand this part later.</li> </ol>"},{"location":"FPGA/rx_dma_config/","title":"rx_dma_config","text":"<p>On one side, <code>rx_dma_config</code> connects to cq_parser and cc_formatter to connect to the host. On the other side, it connects to rx_dma_stage, guiding the DMA process.</p>"},{"location":"FPGA/rx_dma_config/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/DMA_related/rx_dma_config.v</code>.</p>"},{"location":"FPGA/rx_dma_config/#design-logic","title":"Design Logic","text":"<p>This module saves the offset address of different registers, they are:</p> <pre><code>localparam [BAR0_SIZE-1:0] REG_RESET                        = 16'h00;\nlocalparam [BAR0_SIZE-1:0] REG_ID                           = 16'h04;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_SLOT_NUM_OFFSET       = 16'h08;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_ENABLE_OFFSET         = 16'h10;\nlocalparam [BAR0_SIZE-1:0] REG_STATUS                       = 16'h0C;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_CONS_PTR_OFFSET       = 16'h18;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_PROD_OFFSET           = 16'h20;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_DROP_OFFSET           = 16'h28;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_STAT_OFFSET           = 16'h30;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_BASE0                 = 16'h40;\nlocalparam [BAR0_SIZE-1:0] REG_RX_QUE_STRIDE                = 16'h40;\n</code></pre> <ul> <li><code>REG_RESET (0x00)</code>: Write any value to generate a one-cycle <code>o_reg_reset</code> pulse and clear the stored queue base address, slot count, and consumer pointer state.</li> <li><code>REG_ID (0x04)</code>: Read-only module identifier register; returns <code>64'h4d5f52585f434647</code>.</li> <li><code>REG_RX_QUE_SLOT_NUM_OFFSET (0x08)</code>: Per-queue offset for the slot-count register, which software writes to define the host ring depth; together with the consumer pointer, the NIC uses it to determine whether the queue is full and whether another DMA write can be issued.</li> <li><code>REG_RX_QUE_ENABLE_OFFSET (0x10)</code>: Per-queue offset for the derived enable bit; it reads as <code>1</code> only when both the queue base address and slot count are non-zero.</li> <li><code>REG_STATUS (0x0C)</code>: Global status register; currently it always reads back <code>0</code>. (Not used now)</li> <li><code>REG_RX_QUE_CONS_PTR_OFFSET (0x18)</code>: Per-queue offset for the consumer pointer register written by software to indicate which descriptors have been consumed.</li> <li><code>REG_RX_QUE_PROD_OFFSET (0x20)</code>: Per-queue offset for the producer pointer register reported by <code>rx_dma_stage</code> on reads.</li> <li><code>REG_RX_QUE_DROP_OFFSET (0x28)</code>: Per-queue offset for the drop counter reported by <code>rx_dma_stage</code> on reads.</li> <li><code>REG_RX_QUE_STAT_OFFSET (0x30)</code>: Per-queue offset for the queue status word reported by <code>rx_dma_stage</code> on reads. The current bit layout is bit 0 <code>enabled</code>, bit 1 <code>full</code>, and bit 2 <code>busy</code>.</li> <li><code>REG_RX_QUE_BASE0 (0x40)</code>: Base address of queue 0's register window; writing here stores the descriptor ring base address for that queue.</li> <li><code>REG_RX_QUE_STRIDE (0x40)</code>: Spacing between queue register windows, so queue <code>n</code> starts at <code>0x40 + n * 0x40</code>.</li> </ul>"},{"location":"FPGA/rx_dma_config/#how-to-coordinate-with-the-host","title":"How to coordinate with the host","text":"<p>This module is for MMIO operation, which means the host is always the initiator. As this module is for control purpose, host can do write and read to the registers in this module</p> <p>Below gives two example on write and read, respectively.</p> <p></p> <p>One has to note that different from the general NIC operation, which takes two steps, there is only one step to start to DMA write in this design</p> <p>In regular NIC, the host fills the descriptors, sends the IOVA of the descriptors to the NIC, from which the NIC takes out the starting address of the real packets and start to read/write.</p> <p>In this NIC, the host directly write the starting address with MMIO to the NIC. This is due to the packet size is fixed to 256-bit.</p>"},{"location":"FPGA/rx_dma_config/#connection-with-rx_dma_stage","title":"Connection with rx_dma_stage","text":"<p>Not all the registers acting with the host are in this module. Some of them are in rx_dma_stage, as they are directly related to DMA operation. Below shows the main registers in the two modules. Among them, <code>reg_que_base</code>, <code>reg_que_slot_num</code> and <code>reg_que_cons</code> are in rx_dma_config, as they are directly configured by the host through MMIO. Others are in rx_dma_stage, since they vary in the process of DMA writing.</p> <p></p>"},{"location":"FPGA/rx_dma_config/#pointers","title":"Pointers","text":"<p>Everytime there is a packet having been sent to the host via DMA, <code>que_prod_ptr</code> will <code>+1</code>. Similarly, once the host has taken out the packet, it has to write back the current <code>reg_que_cons</code> through MMIO to notify the NIC (otherwise the NIC does not know the whether the memory pool is full or not, keep writing can overwrite the previous packets in the host). They are monotonic registers, with 64-bit width, widely enough for packets. Once they meet, it indicates the memory pool in the host is full, NIC starts to drop packets and increase <code>que_drop_count</code>.</p>"},{"location":"FPGA/rx_dma_config/#address","title":"Address","text":"<p><code>que_slot_index</code> is for calculating the DMA writing address, together with <code>i_que_base_addr</code> and <code>i_que_slot_num</code>. Because there is a boundary in the memory pool, <code>que_slot_index</code> will be wrapped to <code>0</code> once it hits <code>i_que_slot_num</code>. Below illustrates this behavior.</p> <p></p>"},{"location":"FPGA/rx_dma_config/#reset-register","title":"Reset Register","text":"<p>The block also exports <code>o_reg_reset</code> (not shown in the figure), a software-triggered reset pulse used by <code>rx_dma_stage</code> to clear queue runtime state such as producer pointers and drop counters.</p>"},{"location":"FPGA/rx_dma_config/#notes","title":"Notes","text":"<ul> <li>The enable register is currently derived status, not an independently stored control bit.</li> <li>Runtime counters live in <code>rx_dma_stage</code>; <code>rx_dma_config</code> only exposes them on MMIO reads.</li> </ul>"},{"location":"FPGA/order_book_builder/overview/","title":"order_book_builder","text":"<p><code>order_book_builder</code> receives parsed ITCH messages from order_book_parser and builds one independent top-of-book pipeline per tracked symbol.</p>"},{"location":"FPGA/order_book_builder/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/order_book_builder.v</code>.</p>"},{"location":"FPGA/order_book_builder/overview/#design-logic","title":"Design Logic","text":"<p>The module fans one parser event out to all configured symbol_book instances.</p> <p>The hierarchy can be found below:</p> <p></p>"},{"location":"FPGA/order_book_builder/overview/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/overview/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code>: main order-book clock.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_msg_valid</code>: parser output valid.</li> <li><code>i_msg_type</code>: ITCH message type.</li> <li><code>i_stock_locate</code>: parser stock locate.</li> <li><code>i_order_ref_num</code>: original order reference.</li> <li><code>i_new_order_ref_num</code>: replacement order reference for <code>U</code>.</li> <li><code>i_buy_sell</code>: ASCII side field for add-style messages.</li> <li><code>i_shares</code>: message quantity.</li> <li><code>i_price</code>: message price.</li> </ul>"},{"location":"FPGA/order_book_builder/overview/#outputs","title":"Outputs","text":"<ul> <li><code>o_event_valid</code>: one bit per configured symbol. A bit pulses when that <code>symbol_book</code> finishes an update that changes top-of-book.</li> <li><code>o_event_payload</code>: concatenated per-symbol payload bus.</li> </ul>"},{"location":"FPGA/order_book_builder/overview/#payload-layout","title":"Payload Layout","text":"<p>Each symbol payload is <code>208</code> bits wide:</p> <pre><code>{ask_price[31:0], ask_shares[31:0], bid_price[31:0], bid_shares[31:0], event_timestamp[63:0], stock_locate[15:0]}\n</code></pre> <p>This is the same payload that later crosses the CDC FIFO and is zero-padded into the <code>32-byte</code> host record used by rx_dma_stage.</p>"},{"location":"FPGA/order_book_builder/overview/#per-symbol-behavior","title":"Per-Symbol Behavior","text":"<p>The current implementation instantiates two <code>symbol_book</code>s:</p> <ul> <li>AAPL with <code>STOCK_LOCATE = 16'h000d</code></li> <li>HSBC with <code>STOCK_LOCATE = 16'h0ee8</code></li> </ul> <p>That list is a top-level integration choice, not a limit of the module interface.</p>"},{"location":"FPGA/order_book_builder/overview/#limits","title":"Limits","text":"<ul> <li>The event output is top-of-book only. Full depth is not exported.</li> <li>The symbol count and stock-locate mapping are compile-time parameters in the current top-level integration.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/","title":"fifo","text":"<p><code>fifo</code> is the small synchronous FIFO used in several order-book helper blocks.</p>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/fifo.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/#design-logic","title":"Design Logic","text":"<p>This FIFO stores one word per entry in a simple single-clock queue.</p> <p>It exposes:</p> <ul> <li>push request plus <code>o_push_ready</code></li> <li>pop request</li> <li><code>o_not_empty</code></li> <li>registered <code>o_data</code></li> <li>one-cycle <code>o_valid</code> pulse when a pop returns data</li> </ul> <p>The current implementation is used as a short decoupling queue inside book_builder and qty_builder.</p>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/#timing-behavior","title":"Timing Behavior","text":"<p>The important behavior is on the pop side:</p> <ul> <li><code>i_do_pop</code> requests one pop</li> <li>on that clock edge, the FIFO copies <code>mem[rd_ptr]</code> into <code>o_data</code></li> <li><code>o_valid</code> is asserted for that cycle after the register update</li> </ul> <p>So <code>o_data</code> is meaningful when <code>o_valid = 1</code>, not merely when <code>i_do_pop = 1</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/#limits","title":"Limits","text":"<ul> <li><code>o_push_ready</code> is not checked everywhere the FIFO is instantiated, so overflow handling is still a system-level concern.</li> <li>The reset is synchronous to <code>i_clk</code> in this module.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/overview/","title":"symbol_book","text":"<p><code>symbol_book</code> is the per-symbol order-book engine used by order_book_builder.</p>"},{"location":"FPGA/order_book_builder/symbol_book/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/symbol_book.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/overview/#design-logic","title":"Design Logic","text":"<p>Each instance tracks one configured <code>STOCK_LOCATE</code>.</p> <p>The incoming parser message is always present on <code>i_parser_msg</code>, but only matching traffic is allowed to update the internal order table because <code>book_builder</code> receives <code>i_stock_valid</code>.</p> <p>The module has four active blocks:</p> <ol> <li>book_builder: maintains the order-reference table and emits compact quantity updates.</li> <li>ask-side qty_book_wrapper: tracks ask price levels.</li> <li>bid-side qty_book_wrapper: tracks bid price levels.</li> <li><code>timestamper</code>: captures a timestamp used in the exported event payload.</li> </ol> <p><code>symbol_book</code> also compares the latest best bid and ask against the previous cycle. When either side changes and the aligned <code>op_done</code> pulse says the update is complete, <code>o_event_found</code> pulses and <code>o_payload</code> carries the new snapshot.</p> <p>Below gives the inside structure:</p> <p></p>"},{"location":"FPGA/order_book_builder/symbol_book/overview/#payload","title":"Payload","text":"<p>The exported symbol payload is:</p> <pre><code>{ask_price[31:0], ask_shares[31:0], bid_price[31:0], bid_shares[31:0], event_timestamp[63:0], stock_locate[15:0]}\n</code></pre> <p>An empty side is represented by zero shares, and with the current <code>PRICE_BASE = 0</code> integration the corresponding price also collapses to zero.</p>"},{"location":"FPGA/order_book_builder/symbol_book/overview/#why-the-output-is-atomic","title":"Why The Output Is Atomic","text":"<p><code>book_builder</code> may need multiple internal cycles to complete one parser message. <code>qty_book_wrapper</code> and <code>tree_builder</code> add more latency on the price-level path.</p> <p>The exported event path uses the aligned <code>op_done</code> pulse from the ask or bid wrapper:</p> <ul> <li>internal quantity updates can take several cycles</li> <li>the final outward <code>o_event_found</code> pulse appears only when the aligned result is ready</li> </ul> <p>That is why the host-visible event stream behaves atomically even though the internal update pipeline is multi-cycle.</p>"},{"location":"FPGA/order_book_builder/symbol_book/overview/#limits","title":"Limits","text":"<ul> <li>The module exports only top-of-book state, not the full order table or all price levels.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/","title":"bram","text":"<p>This module is the single-port storage used by book_builder as <code>order_book_inst</code>. It keeps the current live order state for one symbol, so later cancel, execute, delete, or replace messages can find the old side, shares, and price.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#introduction","title":"Introduction","text":"<p>In <code>book_builder</code>, it is instantiated with <code>ADDR_WIDTH = BOOK_LEVEL_BIT</code> and <code>DATA_WIDTH = 67</code>, so each entry stores <code>{valid, side, shares, price}</code> for one hashed order reference.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#design-logic","title":"Design Logic","text":"<p>The module is intentionally small:</p> <p>BRAM is a dedicated on-chip memory block provided by the FPGA fabric, separate from ordinary flip-flop registers and LUTs. It is used here because the order table is naturally a memory array: with <code>BOOK_LEVEL_BIT = 12</code>, the design needs <code>4096</code> (can adjust) addressable rows, and each row stores <code>67</code> bits of live-order state. Implementing that table with BRAM is much more area-efficient than building the same storage from registers.</p> <p>The tradeoff is that read and write are clocked operations. In this module, a read result appears on <code>o_data</code> after the next rising edge, so book_builder uses the <code>FIRST_CYCLE</code> and <code>SECOND_CYCLE</code> states before it consumes <code>book_o_data</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code>: BRAM clock.</li> <li><code>i_rst</code>: clears only the output register <code>o_data</code>.</li> <li><code>i_addr</code>: row address.</li> <li><code>i_op</code>: operation selector, where <code>2'b01</code> is read and <code>2'b10</code> is write.</li> <li><code>i_data</code>: row payload to write.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#outputs","title":"Outputs","text":"<ul> <li><code>o_data</code>: registered read data.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#stored-format","title":"Stored Format","text":"<p>Inside book_builder, the row width is <code>67</code> bits:</p> <pre><code>{valid, side[1:0], shares[31:0], price[31:0]}\n</code></pre> <p>Example:</p> <pre><code>{1'b1, 2'b01, 32'd100, 32'd200}\n</code></pre> <p>This means the slot is valid, the order is on the bid side, the resting quantity is <code>100</code>, and the price is <code>200</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#read-and-write-behavior","title":"Read And Write Behavior","text":"<ul> <li><code>WRITE</code>: on the rising edge, <code>bram[i_addr] &lt;= i_data</code>.</li> <li><code>READ</code>: on the rising edge, <code>o_data &lt;= bram[i_addr]</code>.</li> <li><code>IDLE</code>: memory and output keep their previous values unless reset changes <code>o_data</code>.</li> </ul> <p>This is a single-port BRAM, so one cycle performs one logical operation chosen by <code>i_op</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#reset-behavior","title":"Reset Behavior","text":"<p>Two reset-related details matter:</p> <ol> <li>The <code>initial</code> block fills the memory array with zeros. This helps simulation and also gives FPGA tools a zero-initialized BRAM image when supported.</li> <li>Runtime <code>i_rst</code> does not sweep through the whole memory and clear all rows. It only sets <code>o_data</code> to zero.</li> </ol> <p>Because of that, book_builder removes an order by explicitly writing an all-zero row back into the selected slot.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#addressing","title":"Addressing","text":"<p>This BRAM does not know anything about <code>order_ref_num</code> directly. book_builder computes the address first with <code>cal_order_book_addr()</code> and passes the folded result into <code>i_addr</code>.</p> <p>That means the BRAM itself is just a storage array; the hashing and collision risk live outside this module.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#synthesis-note","title":"Synthesis Note","text":"<p>The array is declared with:</p> <pre><code>(* ram_style = \"block\" *) reg [DATA_WIDTH-1:0] bram [0:(1&lt;&lt;ADDR_WIDTH)-1];\n</code></pre> <p>The attribute asks the synthesis tool to map this storage to block RAM instead of distributed LUT RAM when possible.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#limits","title":"Limits","text":"<ul> <li>There is no byte enable or partial update path; every write replaces the full row.</li> <li>There is no explicit valid-ready handshake on the output; the caller must already understand the one-cycle read latency.</li> <li>The module does not detect address collisions. If two order references hash to the same <code>i_addr</code>, the later write overwrites the earlier row.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/","title":"book_builder","text":"<p><code>book_builder</code> is the order-reference stage inside symbol_book.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/book_builder.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#design-logic","title":"Design Logic","text":"<p>The module keeps one hashed order table in bram and translates parser messages into compact quantity updates qty_book_wrapper at the same time. for the downstream bid and ask quantity books.</p> <p>Below shows the structure of book_builder module.</p> <p></p> <p>The public output is:</p> <pre><code>o_qty_msg = {bid_ask[1:0], price[31:0], is_add, d_shares[31:0], op_done}\n</code></pre> <p><code>op_done</code> marks when the exported quantity update represents a completed book operation from the point of view of the outward top-of-book stream. This is useful for <code>TYPE_U</code> message, as it requires two separate book operation.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#parser-input-format","title":"Parser Input Format","text":"<p><code>i_parser_msg</code> carries:</p> <pre><code>{msg_valid, msg_type, stock_locate, order_ref_num, new_order_ref_num, side, shares, price}\n</code></pre> <p>Only messages with <code>i_stock_valid = 1</code> are pushed into the internal FIFO.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#state-machine","title":"State Machine","text":"<p>The module uses a small FSM because the order table BRAM has synchronous read latency.</p> <ul> <li><code>IDLE</code>: handle <code>A/F</code> immediately or start a read for <code>D/X/E/C/U</code>.</li> <li><code>FIRST_CYCLE</code>: keep the read active.</li> <li><code>SECOND_CYCLE</code>: consume the read result and perform delete, reduce, or the remove phase of <code>U</code>.</li> <li><code>THIRD_CYCLE</code>: finish the add phase of <code>U</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#order-table","title":"Order Table","text":"<p>Each BRAM entry stores:</p> <pre><code>{valid, side[1:0], shares[31:0], price[31:0]}\n</code></pre> <p>The address is derived from <code>order_ref_num</code> by the XOR-folding helper <code>cal_order_book_addr()</code>. This keeps the table compact but allows collisions.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#limits","title":"Limits","text":"<ul> <li>The internal FIFO push-ready signal is not consumed, so sustained overflow can drop parser events.</li> <li><code>cal_order_book_addr()</code> is a hash, not a collision-free mapping.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/","title":"aligner","text":"<p><code>aligner</code> is the final delay stage inside qty_book_wrapper.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/aligner.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/#design-logic","title":"Design Logic","text":"<p>The best-price path and the BRAM best-share read do not arrive in the same cycle:</p> <ul> <li><code>tree_builder</code> determines the best price index</li> <li>BRAM port B returns the shares for that level one cycle later</li> </ul> <p><code>aligner</code> delays:</p> <ul> <li>best price</li> <li>completion pulse</li> </ul> <p>so they line up with the returned best-share value.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/#inputs","title":"Inputs","text":"<ul> <li><code>i_best_price</code></li> <li><code>i_best_shares</code></li> <li><code>i_t_op_done</code></li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/#outputs","title":"Outputs","text":"<ul> <li><code>o_best_price_aligned</code></li> <li><code>o_best_shares</code></li> <li><code>o_op_done_aligned</code></li> </ul> <p>There is no explicit valid bit in the current module.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/","title":"bram_dp","text":"<p>This module is the dual-port storage used inside qty_book_wrapper. It stores the live total shares at every tracked price level and allows the wrapper to update one price level while reading the shares of the current best price level at the same time.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#design-logic","title":"Design Logic","text":"<p>This storage is used because qty_book_wrapper needs two memory accesses in parallel:</p> <ol> <li>port A is used by qty_builder to read the old quantity of the updated price level and then write back the new total shares</li> <li>port B is used to read the shares stored at the current best price index selected by tree_builder</li> </ol> <p>Using a dual-port BRAM keeps the full quantity book in one memory block and avoids duplicating storage just to support those two access patterns.</p> <p>The tradeoff is the same as with ordinary FPGA BRAM: both reads and writes are clocked operations. A read result appears on <code>o_data_a</code> or <code>o_data_b</code> after the next rising edge, so aligner is needed to line up <code>best_valid</code> and <code>best_price</code> with the returned best-share data.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code>: BRAM clock.</li> <li><code>i_rst</code>: clears the two output registers.</li> <li><code>i_addr_a</code>: address for port A.</li> <li><code>i_op_a</code>: operation for port A, where <code>2'b01</code> is read and <code>2'b10</code> is write.</li> <li><code>i_data_a</code>: write data for port A.</li> <li><code>i_addr_b</code>: address for port B.</li> <li><code>i_op_b</code>: operation for port B, where <code>2'b01</code> is read and <code>2'b10</code> is write.</li> <li><code>i_data_b</code>: write data for port B.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#outputs","title":"Outputs","text":"<ul> <li><code>o_data_a</code>: registered read data from port A.</li> <li><code>o_data_b</code>: registered read data from port B.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#stored-format","title":"Stored Format","text":"<p>Unlike the order-table BRAM in book_builder/bram.md, this memory does not store full order state. Each row stores only the aggregated shares of one price level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#read-and-write-behavior","title":"Read And Write Behavior","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#limits","title":"Limits","text":"<ul> <li>There is no byte enable or partial write path.</li> <li>The outputs are registered, so users of this module must account for read latency explicitly.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/","title":"qty_book_wrapper","text":"<p><code>qty_book_wrapper</code> maintains one side of the price-level quantity book for one symbol.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/qty_book_wrapper.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#design-logic","title":"Design Logic","text":"<p>Each symbol_book instantiates two wrappers:</p> <ul> <li>one ask-side wrapper</li> <li>one bid-side wrapper</li> </ul> <p>Both wrappers observe the same <code>i_qty_msg</code> stream from book_builder, but each wrapper keeps only the messages whose side matches <code>BID_OR_ASK</code>.</p> <p>The wrapper splits the work into four blocks:</p> <ol> <li>qty_builder: read-modify-write update for one touched price level.</li> <li>tree_builder: track which price indices are currently non-empty and pick the best one.</li> <li>aligner: delay the best-price path so it lines up with the BRAM read result and completion pulse.</li> <li>bram_dp: hold the aggregated shares for all tracked price levels.</li> </ol> <p>These modules and the connections can be found in the figure below:</p> <p></p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code>: main clock.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_qty_msg</code>: <code>{bid_ask, price, is_add, d_shares, op_done}</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#outputs","title":"Outputs","text":"<ul> <li><code>o_best_price_aligned</code>: aligned best price for this side.</li> <li><code>o_best_shares</code>: aligned aggregated shares at that best price.</li> <li><code>o_op_done_aligned</code>: aligned completion pulse for the current outward event.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#data-path","title":"Data Path","text":"<p>The wrapper updates the touched price level through BRAM port A and reads the current best level through BRAM port B.</p> <p><code>tree_builder</code> stores only occupancy, not exact quantities. Exact shares stay in BRAM.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#limits","title":"Limits","text":"<ul> <li>Prices below <code>PRICE_BASE</code> or beyond the tracked depth collapse to index <code>0</code>.</li> <li>The wrapper boundary has no backpressure signal.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/","title":"qty_builder","text":"<p><code>qty_builder</code> is the update engine inside qty_book_wrapper.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/qty_builder.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#design-logic","title":"Design Logic","text":"<p>The module receives quantity deltas, filters them by side, and updates one aggregated price level in the external bram_dp.</p> <p>It uses an internal fifo because one update requires a BRAM read-modify-write sequence:</p> <ol> <li>pop one matching quantity message</li> <li>translate <code>price</code> to a compact price index</li> <li>issue a BRAM read</li> <li>wait for the synchronous read result</li> <li>compute the new share total</li> <li>issue the BRAM write</li> </ol> <p>At the write phase it also checks whether the price level changed occupancy:</p> <ul> <li><code>0 -&gt; non-zero</code></li> <li><code>non-zero -&gt; 0</code></li> </ul> <p>Only those occupancy changes are forwarded to tree_builder.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code>: main clock.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_qty_msg</code>: <code>{bid_ask, price, is_add, d_shares, op_done}</code>.</li> <li><code>i_bram_o_data</code>: current quantity read from BRAM port A.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#outputs","title":"Outputs","text":"<ul> <li><code>o_price_idx</code>: price index whose occupancy changed.</li> <li><code>o_price_change</code>: <code>NON_EMPTY</code>, <code>EMPTY</code>, or <code>IDLE</code>.</li> <li><code>o_op_done</code>: completion flag aligned to the occupancy-change path.</li> <li><code>o_bram_addr</code>: BRAM address for port A.</li> <li><code>o_bram_op</code>: BRAM operation.</li> <li><code>o_bram_i_data</code>: updated share total written back to BRAM.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#side-filtering","title":"Side Filtering","text":"<p>The current side filter is purely based on the side field:</p> <pre><code>wire ff_push = i_qty_side == BID_OR_ASK;\n</code></pre> <p>So each wrapper sees the same bus but only enqueues messages for its own side.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#price-mapping","title":"Price Mapping","text":"<p>The helper <code>cal_qty_book_addr()</code> converts an absolute price into a compact level index:</p> <pre><code>price_idx = (price - PRICE_BASE) &gt;&gt; 2\n</code></pre> <p>That means one stored level corresponds to a price step of <code>4</code> in the encoded price unit.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#limits","title":"Limits","text":"<ul> <li>The FIFO push-ready signal is not consumed here, so overflow must be handled at system level.</li> <li>Out-of-range prices collapse to index <code>0</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/","title":"tree_builder","text":"<p><code>tree_builder</code> tracks which price levels are currently non-empty and returns the best price index for one side of the book.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/tree_builder.v</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#design-logic","title":"Design Logic","text":"<p>The module stores one valid bit per tracked price level and reduces that occupancy information into one best index.</p> <p>The update input comes from qty_builder:</p> <ul> <li><code>NON_EMPTY</code>: level changed from empty to non-empty</li> <li><code>EMPTY</code>: level changed from non-empty to empty</li> <li><code>IDLE</code>: no occupancy change</li> </ul> <p>The reduction rule depends on <code>BID_OR_ASK</code>:</p> <ul> <li>bid side chooses the higher valid index</li> <li>ask side chooses the lower valid index</li> </ul> <p>The implementation uses a mixed combinational/sequential tree. The lower half of the tree is reduced combinationally, one mid-level stage is registered, and the upper half is reduced combinationally again. That shortens the update path compared with an all-register tree.</p> <p>Below gives a schematic of the tree structure, refer to HFT Accelerator</p> <p></p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code></li> <li><code>i_rst</code></li> <li><code>i_price_idx</code></li> <li><code>i_price_change</code></li> <li><code>i_op_done</code></li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#outputs","title":"Outputs","text":"<ul> <li><code>o_best_price_idx</code></li> <li><code>o_op_done</code></li> </ul> <p>There is no separate <code>o_best_valid</code> signal in the current RTL. When no level is active, the selected index collapses to <code>0</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#output-use","title":"Output Use","text":"<p>qty_book_wrapper converts <code>o_best_price_idx</code> back into an absolute price and uses BRAM port B to fetch the shares stored at that best level.</p>"},{"location":"FPGA/pcie_wrapper/CC_formatter/","title":"CC_formatter","text":"<p><code>CC_formatter</code> packs signals from the user logic side to the PCIe IP core. Since it is mainly for control signals, it only supports single-beat transfer for simplicity.</p>"},{"location":"FPGA/pcie_wrapper/CC_formatter/#design-logic","title":"Design Logic","text":"<p>The user logic provides the metadata needed for a PCIe completion:</p> <ul> <li>requester ID</li> <li>tag</li> <li>traffic class</li> <li>lower address</li> <li>completion status</li> <li>payload DWord count</li> <li>up to <code>128 bits</code> of read data (the payload)</li> </ul> <p><code>CC_formatter</code> packs those fields into the <code>96-bit</code> completion descriptor used on the <code>CC</code> channel and concatenates it with the payload:</p> <pre><code>s_axis_cc_tdata = {32'h0, cc_payload[127:0], descriptor[95:0]}\n</code></pre>"},{"location":"FPGA/pcie_wrapper/CC_formatter/#interface","title":"Interface","text":""},{"location":"FPGA/pcie_wrapper/CC_formatter/#inputs","title":"Inputs","text":"<ul> <li><code>cc_valid</code>: completion valid from user logic.</li> <li><code>cc_requester_id</code>: requester ID copied from the original host request.</li> <li><code>cc_tag</code>: tag copied from the original host request.</li> <li><code>cc_tc</code>: traffic class.</li> <li><code>cc_lower_addr</code>: lower address bits used by the completion descriptor.</li> <li><code>cc_dword_count</code>: completion payload size in DWords.</li> <li><code>cc_status</code>: completion status code.</li> <li><code>cc_payload</code>: up to <code>128 bits</code> of read response data.</li> <li><code>cc_last</code>: last beat of the completion. In the current implementation it should always be asserted for a supported transfer.</li> <li><code>s_axis_cc_tready</code>: ready from the PCIe IP core.</li> </ul>"},{"location":"FPGA/pcie_wrapper/CC_formatter/#outputs","title":"Outputs","text":"<ul> <li><code>cc_ready</code>: ready back to user logic. This is <code>s_axis_cc_tready[0]</code>.</li> <li><code>s_axis_cc_tdata</code></li> <li><code>s_axis_cc_tvalid</code></li> <li><code>s_axis_cc_tuser</code></li> <li><code>s_axis_cc_tkeep</code></li> <li><code>s_axis_cc_tlast</code></li> </ul>"},{"location":"FPGA/pcie_wrapper/CC_formatter/#keep-and-handshake","title":"Keep And Handshake","text":"<p>The current <code>tkeep</code> logic handles the small completions used by this project:</p> <ul> <li><code>cc_dword_count == 1</code>: <code>tkeep = 8'h0F</code></li> <li><code>cc_dword_count == 2</code>: <code>tkeep = 8'h1F</code></li> <li>otherwise: <code>tkeep = 8'hFF</code></li> </ul> <p><code>tuser</code> is tied to zero in the current RTL, and <code>cc_ready</code> simply forwards the PCIe IP ready bit.</p>"},{"location":"FPGA/pcie_wrapper/CC_formatter/#limits","title":"Limits","text":"<ul> <li>The current module does not support multi-beat completions.</li> <li>The payload interface is limited to <code>128 bits</code>.</li> <li>The code assumes the completion can be represented in one <code>256-bit</code> <code>CC</code> beat.</li> </ul>"},{"location":"FPGA/pcie_wrapper/CQ_parser/","title":"CQ_parser","text":"<p>Its job is to decode the infomation in descriptors from the PCIe IP core, and separate the payload as well. It does not support multi-beat operation, as it is only used for control signals.</p>"},{"location":"FPGA/pcie_wrapper/CQ_parser/#design-logic","title":"Design Logic","text":"<p><code>CQ_parser</code> assumes the PCIe IP presents the request descriptor in the low part of <code>m_axis_cq_tdata</code>. It slices those descriptor fields into project-specific outputs and always asserts ready:</p> <pre><code>assign m_axis_cq_tready = 1'b1;\nassign cq_valid         = m_axis_cq_tvalid;\n</code></pre> <p>The most important fields are:</p> <ul> <li><code>cq_reg_addr</code>: BAR-relative byte address, forced to <code>4-byte</code> alignment</li> <li><code>cq_type</code>: request type used by the project register block</li> <li><code>cq_payload_dw_count</code>: host request length in DWords</li> <li><code>cq_payload</code>: <code>64-bit</code> write data</li> </ul> <p>The current register address mapping is:</p> <pre><code>assign cq_reg_addr = {m_axis_cq_tdata[2 +: (BAR0_SIZE-2)], 2'b00};\n</code></pre> <p>So the parser drops the two LSBs and re-appends <code>2'b00</code>, which means the downstream register block sees aligned byte addresses.</p> <p>For writes, the current payload extraction combines two DWords from the request beat:</p> <pre><code>assign cq_payload = {m_axis_cq_tdata[191:160], m_axis_cq_tdata[159:128]};\n</code></pre> <p>This is the <code>64-bit</code> data field consumed by the MMIO register logic.</p>"},{"location":"FPGA/pcie_wrapper/CQ_parser/#interface","title":"Interface","text":""},{"location":"FPGA/pcie_wrapper/CQ_parser/#inputs","title":"Inputs","text":"<ul> <li><code>m_axis_cq_tdata</code>: raw <code>256-bit</code> CQ beat.</li> <li><code>m_axis_cq_tvalid</code></li> <li><code>m_axis_cq_tuser</code></li> <li><code>m_axis_cq_tkeep</code></li> <li><code>m_axis_cq_tlast</code></li> </ul>"},{"location":"FPGA/pcie_wrapper/CQ_parser/#outputs","title":"Outputs","text":"<ul> <li><code>m_axis_cq_tready</code>: always asserted in the current implementation.</li> <li><code>cq_valid</code>: decoded request valid.</li> <li><code>cq_type</code>: request type.</li> <li><code>cq_reg_addr</code>: aligned BAR-relative register address.</li> <li><code>cq_payload</code>: <code>64-bit</code> write payload.</li> <li><code>cq_bar_id</code>: target BAR ID.</li> <li><code>cq_requester_id</code>: requester ID of the root complex.</li> <li><code>cq_tag</code>: request tag.</li> <li><code>cq_tc</code>: traffic class.</li> <li><code>cq_lower_addr</code>: lower address bits used later by CC_formatter.</li> <li><code>cq_payload_dw_count</code>: request length in DWords.</li> <li><code>cq_last</code>: forwarded last-beat indication.</li> </ul>"},{"location":"FPGA/pcie_wrapper/CQ_parser/#limits","title":"Limits","text":"<ul> <li><code>CQ_parser</code> is effectively a single-beat parser in the current project usage.</li> <li><code>m_axis_cq_tuser</code> and <code>m_axis_cq_tkeep</code> are not used by the current RTL.</li> <li>The parser always asserts ready, so there is no backpressure on the <code>CQ</code> channel.</li> </ul>"},{"location":"FPGA/pcie_wrapper/overview/","title":"pcie_wrapper","text":"<p>This module connects the Xilinx UltraScale PCIe Gen3 IP core to the rest of the design. It is the top-level PCIe integration block and exposes a simpler logic-side interface for MMIO and requester traffic.</p>"},{"location":"FPGA/pcie_wrapper/overview/#design-logic","title":"Design Logic","text":"<p>The implementation is in <code>verilog_src/PCIe_related/pcie_wrapper.v</code>.</p> <p>The main function is to separate the descriptors from the payloads, as they are packed together by the PCIe GEN3 IP core originally.</p> <p>There are four paths from the IP core:</p> <ul> <li><code>CQ</code> and <code>CC</code> are used for MMIO-style control transactions, where the host (PC side, CPU core) is the initiator</li> <li><code>RQ</code> and <code>RC</code> are used for requester traffic such as DMA-style transfers, where the user logic (FPGA side) is the initiator.</li> </ul> <p>The detailed behavior of each path is documented in the corresponding subpages.</p>"},{"location":"FPGA/pcie_wrapper/overview/#hierarchy","title":"Hierarchy","text":"<p>The internal structure is shown below:</p> <p></p> <p>The main submodules are:</p> <ol> <li><code>pcie3_ultrascale_0</code>: PCIe hard IP wrapper</li> <li>CQ_parser: decodes the <code>CQ</code> channel</li> <li>CC_formatter: formats the <code>CC</code> channel</li> <li>RQ_formatter: formats the <code>RQ</code> channel</li> <li>RC_parser: parses the <code>RC</code> channel</li> </ol>"},{"location":"FPGA/pcie_wrapper/overview/#clock-and-reset","title":"Clock And Reset","text":"<p>The PCIe IP generates the user-side signals exported by the wrapper:</p> <ul> <li><code>o_user_clk_250</code></li> <li><code>o_user_reset_p</code></li> </ul> <p>This <code>250 MHz</code> clock domain is used by the PCIe-side logic in the design.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/","title":"RC_gearbox256","text":"<p><code>RC_gearbox256</code> realigns the PCIe <code>RC</code> completion payload stream so user logic sees a consistent <code>256-bit</code> payload channel.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/PCIe_related/dependencies/RC_gearbox256.v</code>.</p> <p>Inside RC_parser, this module receives raw <code>m_axis_rc_*</code> beats from the PCIe IP core and produces:</p> <ul> <li><code>rc_descriptor</code>: the <code>96-bit</code> completion descriptor captured on SOP</li> <li><code>rc_payload</code>: a realigned <code>256-bit</code> payload bus</li> <li><code>rc_valid</code>, <code>rc_payload_last</code>, and <code>rc_payload_dw_keep</code></li> </ul>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#design-logic","title":"Design Logic","text":"<p>The first beat of an <code>RC</code> completion is not payload-only. Its lower <code>96 bits</code> carry the completion descriptor, while the upper portion carries payload. Later beats are payload-only.</p> <p>That means the payload stream is shifted at the packet boundary. <code>RC_gearbox256</code> fixes that by saving the upper <code>160 bits</code> of the current beat in <code>data_saver</code> and combining them with the lower <code>96 bits</code> of the following beat:</p> <pre><code>assign rc_payload = {m_axis_rc_tdata[95:0], data_saver};\n</code></pre> <p>So the gearbox converts the raw RC beat layout into one stable <code>256-bit</code> payload word stream for logic.</p> <p>On SOP, the module also captures:</p> <ul> <li>the <code>96-bit</code> completion descriptor</li> <li>the total completion payload DWord count</li> <li>a cached tail keep mask for the last beat</li> </ul> <p><code>m_axis_rc_tuser[32]</code> is used as the SOP indicator in the current RTL.</p> <p>Below shows the design logic in a figure:</p> <p></p>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#payload-last-and-keep","title":"Payload Last And Keep","text":"<p>The module decides the output tail condition from the completion DWord count.</p> <p>There are two main cases:</p> <ol> <li>Short completion: if the total payload is fewer than <code>8</code> DWords, the current beat is also the last beat, and <code>rc_payload_dw_keep</code> is generated directly from the completion length.</li> <li>Multi-beat completion: intermediate beats use <code>8'hFF</code>, and the final beat uses the keep pattern cached in <code>rc_last_keep</code>.</li> </ol> <p>The helper <code>calc_tail_keep()</code> maps a completion length in DWords to the final <code>8-bit</code> keep mask.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#interface","title":"Interface","text":""},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#inputs","title":"Inputs","text":"<ul> <li><code>clk</code>: PCIe user clock.</li> <li><code>rst_n</code>: active-low reset.</li> <li><code>m_axis_rc_tdata</code></li> <li><code>m_axis_rc_tvalid</code></li> <li><code>m_axis_rc_tuser</code></li> <li><code>m_axis_rc_tkeep</code></li> <li><code>m_axis_rc_tlast</code></li> </ul>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#outputs","title":"Outputs","text":"<ul> <li><code>m_axis_rc_tready</code>: always <code>1'b1</code> in the current implementation.</li> <li><code>rc_valid</code></li> <li><code>rc_payload_last</code></li> <li><code>rc_payload</code></li> <li><code>rc_payload_dw_keep</code></li> <li><code>rc_descriptor</code></li> </ul>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#handshake","title":"Handshake","text":"<p>The current gearbox is always ready:</p> <pre><code>assign m_axis_rc_tready = 1'b1;\n</code></pre> <p>So every valid RC beat is accepted immediately. There is no internal stall path in the current implementation.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/RC_gearbox256/#limits","title":"Limits","text":"<ul> <li>The implementation is written for <code>DATA_WIDTH = 256</code>.</li> <li>The logic assumes a <code>96-bit</code> descriptor in the low part of the SOP beat.</li> <li><code>m_axis_rc_tkeep</code> is present on the interface but is not used by the current realignment logic.</li> </ul>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/","title":"RC_parser","text":"<p><code>RC_parser</code> is the receive-side PCIe completion parser for the <code>RC</code> AXI-Stream channel. In this design it is the FPGA block that turns host-to-FPGA DMA read completions into a simpler logic-side descriptor plus payload interface.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/PCIe_related/dependencies/RC_parser.v</code>.</p> <p>Inside pcie_wrapper, this module sits on the <code>RC</code> path:</p> <ul> <li>input side: raw <code>m_axis_rc_*</code> completion beats from the PCIe IP core</li> <li>output side: parsed completion descriptor fields plus a realigned <code>256-bit</code> payload stream</li> </ul> <p><code>RC_parser</code> itself is thin. It delegates beat realignment to RC_gearbox256 and then slices the <code>96-bit</code> completion descriptor into named fields.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#design-logic","title":"Design Logic","text":"<p>The PCIe IP does not present the <code>RC</code> stream in a shape that is convenient for user logic:</p> <ul> <li>the start-of-packet beat contains both a completion descriptor and payload data</li> <li>later beats contain only payload</li> <li>the payload position is therefore shifted relative to the first beat</li> </ul> <p><code>RC_parser</code> handles this in two steps:</p> <ol> <li>RC_gearbox256 removes the beat-to-beat alignment mismatch and produces a stable payload channel</li> <li><code>RC_parser</code> maps the <code>96-bit</code> descriptor into named outputs such as <code>rc_lower_addr</code>, <code>rc_tag</code>, and <code>rc_payload_dw_count</code></li> </ol> <p>The current descriptor field mapping in RTL is:</p> <pre><code>rc_lower_addr         = rc_descriptor[11:0]\nrc_err_code           = rc_descriptor[15:12]\nrc_payload_byte_count = rc_descriptor[28:16]\nrc_request_completed  = rc_descriptor[30]\nrc_payload_dw_count   = rc_descriptor[42:32]\nrc_posioned           = rc_descriptor[46]\nrc_requester_id       = rc_descriptor[63:48]\nrc_tag                = rc_descriptor[71:64]\nrc_completer_id       = rc_descriptor[87:72]\n</code></pre> <p>The signal name <code>rc_posioned</code> is spelled exactly as it appears in the current RTL.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#interface","title":"Interface","text":""},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#inputs","title":"Inputs","text":"<ul> <li><code>clk</code>: PCIe user clock.</li> <li><code>rst_n</code>: active-low reset.</li> <li><code>m_axis_rc_tdata</code>: <code>256-bit</code> raw RC data bus from the PCIe IP.</li> <li><code>m_axis_rc_tvalid</code>: RC beat valid.</li> <li><code>m_axis_rc_tuser</code>: RC sideband bus from the PCIe IP.</li> <li><code>m_axis_rc_tkeep</code>: DWord keep mask from the PCIe IP.</li> <li><code>m_axis_rc_tlast</code>: RC last-beat indication.</li> </ul>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#outputs","title":"Outputs","text":"<ul> <li><code>m_axis_rc_tready</code>: ready back to the PCIe IP. In the current design it is always asserted through the gearbox.</li> <li><code>rc_lower_addr</code></li> <li><code>rc_err_code</code></li> <li><code>rc_payload_byte_count</code></li> <li><code>rc_request_completed</code></li> <li><code>rc_requester_id</code></li> <li><code>rc_completer_id</code></li> <li><code>rc_tag</code></li> <li><code>rc_payload_dw_count</code></li> <li><code>rc_posioned</code></li> <li><code>rc_valid</code>: payload-channel valid after realignment.</li> <li><code>rc_payload_last</code>: last beat of the realigned payload channel.</li> <li><code>rc_payload</code>: realigned <code>256-bit</code> payload output.</li> <li><code>rc_payload_dw_keep</code>: DWord keep mask for <code>rc_payload</code>.</li> </ul>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#relation-to-rc_gearbox256","title":"Relation To RC_gearbox256","text":"<p>The split of work is:</p> <ul> <li>RC_gearbox256: how raw completion beats are realigned into a clean payload stream</li> <li><code>RC_parser</code>: what descriptor metadata is exposed to logic</li> </ul> <p>So <code>RC_parser</code> is the descriptor-unpacking wrapper around the gearbox.</p>"},{"location":"FPGA/pcie_wrapper/RC_parser/overview/#limits","title":"Limits","text":""},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/","title":"RQ_gearbox256","text":"<p>This module packs the requester descriptor and payload stream into the <code>256-bit</code> <code>s_axis_rq_*</code> format required by the PCIe IP core. In this design, it is the beat-level packing stage under RQ_formatter.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/PCIe_related/dependencies/RQ_gearbox256.v</code>.</p> <p>Inside RQ_formatter, this module receives:</p> <ul> <li>a <code>128-bit</code> requester descriptor</li> <li>a <code>256-bit</code> payload stream from user logic</li> <li>packet-boundary signals such as <code>rq_payload_sop</code> and <code>rq_payload_last</code></li> </ul> <p>Its output is the AXI-Stream <code>RQ</code> channel toward the PCIe IP core.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#design-logic","title":"Design Logic","text":"<p>The original PCIe <code>RQ</code> stream does not carry the request descriptor and payload as two separate channels. They must be packed together into <code>256-bit</code> beats with the proper <code>tkeep</code>, <code>tlast</code>, and <code>tuser</code> values.</p> <p><code>RQ_gearbox256</code> performs that packing.</p> <p>At a high level:</p> <ol> <li>place the <code>128-bit</code> descriptor in the lower half of the first beat</li> <li>place payload data beside or after the descriptor depending on packet size</li> <li>save the upper <code>128-bit</code> portion of a payload beat when alignment requires it</li> <li>emit a final partial beat when the remaining payload cannot fit into the previous beat</li> </ol> <p>This is why the module is called a gearbox: it converts the logic-side payload stream into the beat alignment expected by the PCIe <code>RQ</code> interface.</p> <p>Below shows the design logic in a figure:</p> <p></p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#interface","title":"Interface","text":""},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#inputs","title":"Inputs","text":"<ul> <li><code>clk</code>: PCIe user clock</li> <li><code>rst_n</code>: active-low reset</li> <li><code>rq_descriptor</code>: <code>128-bit</code> requester descriptor</li> <li><code>rq_payload</code>: <code>256-bit</code> payload input</li> <li><code>rq_payload_dw_count</code>: total payload size in DWords</li> <li><code>rq_payload_last</code>: final payload beat from user logic</li> <li><code>rq_valid</code>: payload beat valid</li> <li><code>rq_payload_sop</code>: first payload beat</li> <li><code>s_axis_rq_tready</code>: ready from the PCIe IP core</li> </ul>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#outputs","title":"Outputs","text":"<ul> <li><code>rq_ready</code>: ready back to RQ_formatter</li> <li><code>s_axis_rq_tdata</code>: packed <code>256-bit</code> RQ data</li> <li><code>s_axis_rq_tvalid</code></li> <li><code>s_axis_rq_tuser</code></li> <li><code>s_axis_rq_tkeep</code></li> <li><code>s_axis_rq_tlast</code></li> <li></li> </ul>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#relation-to-rq_formatter","title":"Relation To RQ_formatter","text":"<p>The division of work is:</p> <ul> <li>RQ_formatter: builds the requester descriptor</li> <li><code>RQ_gearbox256</code>: packs descriptor and payload into AXI-Stream beats</li> </ul> <p>So <code>RQ_formatter</code> decides what request to send, while <code>RQ_gearbox256</code> decides how that request is placed onto the <code>256-bit</code> PCIe stream.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/RQ_gearbox256/#limits","title":"Limits","text":"<ul> <li>The implementation is written for <code>DATA_WIDTH = 256</code>.</li> <li>The packing logic assumes a <code>128-bit</code> requester descriptor placed only in the first output beat.</li> <li><code>rq_ready</code> is deasserted while an internally generated extra tail beat is pending, so upstream logic must hold its current request state until the gearbox is ready again.</li> </ul>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/","title":"RQ_formatter","text":"<p>This module formats requester requests from user logic into the <code>RQ</code> AXI-Stream interface expected by the PCIe IP core. In this design, it is the transmit-side block for FPGA-initiated host-memory transactions.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/PCIe_related/dependencies/RQ_formatter.v</code>.</p> <p>Inside pcie_wrapper, this module sits on the <code>RQ</code> path:</p> <ul> <li>input side: project-specific request fields from user logic</li> <li>output side: AXI-Stream requester packets to the PCIe IP core</li> </ul> <p><code>RQ_formatter</code> itself mainly builds the requester descriptor. The payload beat packing is handled by RQ_gearbox256.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#design-logic","title":"Design Logic","text":"<p>The module takes a simpler logic-side request interface:</p> <ul> <li>request type</li> <li>host address</li> <li>total payload DWord count</li> <li>tag and traffic class</li> <li>payload data and packet-boundary signals</li> </ul> <p>From those fields, it constructs the <code>128-bit</code> PCIe requester descriptor (only at the first beat) and sends both the descriptor and payload into RQ_gearbox256.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#request-types","title":"Request Types","text":"<p>The module supports the request types used by this design:</p> <ul> <li><code>4'b0000</code>: Memory Read</li> <li><code>4'b0001</code>: Memory Write</li> </ul> <p>For a read request, the outgoing packet contains only the descriptor. For a write request, the descriptor is followed by payload data packed by RQ_gearbox256.</p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#interface","title":"Interface","text":""},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#inputs","title":"Inputs","text":"<ul> <li><code>clk</code>: PCIe user clock</li> <li><code>rst_n</code>: active-low reset</li> <li><code>rq_type</code>: requester type</li> <li><code>rq_addr</code>: host physical address</li> <li><code>rq_payload_dw_count</code>: total payload size in DWords</li> <li><code>rq_tag</code>: requester tag</li> <li><code>rq_tc</code>: traffic class</li> <li><code>rq_valid</code>: request valid</li> <li><code>rq_payload_sop</code>: first payload beat of the request</li> <li><code>rq_payload_last</code>: final payload beat of the request</li> <li><code>rq_payload</code>: <code>256-bit</code> payload input from user logic</li> <li><code>s_axis_rq_tready</code>: ready from the PCIe IP core</li> </ul>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#outputs","title":"Outputs","text":"<ul> <li><code>rq_ready</code>: ready back to user logic</li> <li><code>s_axis_rq_tdata</code></li> <li><code>s_axis_rq_tvalid</code></li> <li><code>s_axis_rq_tuser</code></li> <li><code>s_axis_rq_tkeep</code></li> <li><code>s_axis_rq_tlast</code></li> </ul>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#handshake","title":"Handshake","text":"<p>On the logic side, <code>RQ_formatter</code> uses a <code>valid/ready</code> style interface. Below gives the timing diagram:</p> <p></p>"},{"location":"FPGA/pcie_wrapper/RQ_formatter/overview/#relation-to-rq_gearbox256","title":"Relation To RQ_gearbox256","text":"<p>RQ_gearbox256 performs the actual beat-level packing for the PCIe <code>RQ</code> stream.</p>"},{"location":"FPGA/rx_dma_stage/arbiter/","title":"arbiter","text":"<p><code>arbiter</code> is the small round-robin queue selector used on the FPGA side.</p>"},{"location":"FPGA/rx_dma_stage/arbiter/#introduction","title":"Introduction","text":"<p>The active implementation is in <code>verilog_src/DMA_related/arbiter.v</code>.</p>"},{"location":"FPGA/rx_dma_stage/arbiter/#design-logic","title":"Design Logic","text":"<p>The module accepts one request bit per queue in <code>i_req</code> and chooses one winning queue index in round-robin order.</p> <p>The current requester is remembered in <code>rr_pointer</code>. Each combinational evaluation scans the request vector starting from that pointer and picks the first asserted request.</p> <p>Below illustrates how does the round-robin behave:</p> <p></p> <ol> <li>At time 1, it starts from <code>rr_pointer</code>. <code>rr_adder</code> adds <code>0</code>, <code>1</code>, ..., <code>SYMBOL_NUM</code> to <code>rr_pointer</code> each time. This helps to check whether the CDC_fifo having data to be popped. If it does, it locked the current index to <code>selected_int</code>, also assign <code>o_que_idx</code> to that value.</li> <li>At time 2, once <code>o_que_idx</code> is asserted, a packet will be popped out from the corresponding CDC_fifo. But the following is out of this module's scope.</li> <li>At time 3, <code>rr_pointer</code> will start after <code>o_que_idx</code>, a new loop will automatically start again, until the next valid CDC_fifo</li> </ol>"},{"location":"FPGA/rx_dma_stage/arbiter/#interface","title":"Interface","text":""},{"location":"FPGA/rx_dma_stage/arbiter/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code></li> <li><code>i_rst</code></li> <li><code>i_req</code>: one request bit per queue</li> <li><code>i_accept</code>: handshake that commits the current selection</li> </ul>"},{"location":"FPGA/rx_dma_stage/arbiter/#outputs","title":"Outputs","text":"<ul> <li><code>o_valid</code>: high when at least one request is present</li> <li><code>o_que_idx</code>: selected queue index</li> </ul>"},{"location":"FPGA/rx_dma_stage/arbiter/#current-use","title":"Current Use","text":"<p>In the active RTL, this arbiter is used by rx_dma_stage to choose which per-symbol CDC FIFO should be serviced next.</p>"},{"location":"FPGA/rx_dma_stage/overview/","title":"rx_dma_stage","text":"<p><code>rx_dma_stage</code> is the PCIe-side RX DMA engine that writes fixed-size order-book events into host memory.</p>"},{"location":"FPGA/rx_dma_stage/overview/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/DMA_related/rx_dma_stage.v</code>.</p>"},{"location":"FPGA/rx_dma_stage/overview/#design-logic","title":"Design Logic","text":"<p>This module mainly performs DMA write. It pops out packets from one of the CDC fifos (done by an arbiter module inside), send it to RQ_formatter in the PCIe wrapper. Address calculation is important. This has already been explained in rx_dma_config.</p> <pre><code>write_addr = base_addr + slot_index * 32\n</code></pre>"},{"location":"FPGA/rx_dma_stage/overview/#drop-behavior","title":"Drop Behavior","text":"<p>The module drops an event when the selected queue is disabled or full. This behavior can also be found in rx_dma_config. In that case it: 1. pops the event from the CDC FIFO 2. increments <code>drop_count</code> 3. skips the DMA write This means the receive pipeline keeps moving even when the host ring cannot accept more records.</p>"},{"location":"FPGA/rx_dma_stage/overview/#reset-behavior","title":"Reset Behavior","text":"<p><code>rx_dma_stage</code> also accepts <code>i_reg_reset</code> from rx_dma_config. That synchronous reset path clears the queue producer state and drop counters independently of the global PCIe reset.</p>"},{"location":"FPGA/rx_dma_stage/overview/#limits","title":"Limits","text":"<ul> <li>There is no backpressure toward the upstream event source. Disabled or full queues cause drops.</li> <li>The engine writes fixed-size records only. It is not a descriptor-fetch RX path.</li> </ul>"}]}
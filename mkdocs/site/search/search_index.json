{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to Venturi","text":""},{"location":"#introduction","title":"Introduction","text":"<p>This is a project about order book parsing (Nasdaq ITCH 5.0 protocol) and dummy order sending, out of personal interest.</p>"},{"location":"#it-covers","title":"It covers","text":"<ol> <li>FPGA and a Linux system.</li> <li>Verilog and C++</li> <li>A receive and transmit channel.</li> </ol>"},{"location":"#project-layout","title":"Project layout","text":"<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre>"},{"location":"FPGA/MAC_layer_rx/","title":"MAC_layer_rx","text":"<p><code>MAC_layer_rx</code> converts a 64-bit XGMII receive stream into a simplified AXI-Stream style interface for the parser pipeline. The implementation lives in <code>verilog_src/MAC_layer/MAC_layer_rx.v</code>.</p>"},{"location":"FPGA/MAC_layer_rx/#design-logic","title":"Design logic","text":"<p>From XGMII to MAC layer, the number of cases is limited, and the data is streaming. Thus, we can prepare data for all the possible situations and treat the situations as a switch, output the data when the corresponding situation is true.</p> <p>There can be three states: <code>IDLE</code>, <code>SOF</code> and <code>TERM</code>. <code>SOF</code> means not only the start of the frame, but also the receiving process. In each state, different location of sof and eof can end up with different outputs, these are what we prepared.</p> <p>Combinational logic is widely used for the data preparing.</p>"},{"location":"FPGA/MAC_layer_rx/#interface","title":"Interface","text":""},{"location":"FPGA/MAC_layer_rx/#inputs","title":"Inputs","text":"<ul> <li><code>i_xgmii_rx_clk</code> is the receive clock for the whole module.</li> <li><code>i_xgmii_rx_rst</code> is an active-high reset that clears the current frame state.</li> <li><code>i_xgmii_rxd</code> and <code>i_xgmii_rxc</code> carry one 64-bit XGMII beat plus 8 control bits.</li> <li><code>i_rx_status</code> is treated as link health; if it drops, the module flushes its internal state.</li> <li><code>i_axi_rx_ready</code> is not true backpressure. If the downstream is not ready, the current frame is dropped because XGMII itself cannot stall.</li> </ul>"},{"location":"FPGA/MAC_layer_rx/#outputs","title":"Outputs","text":"<ul> <li><code>o_axi_rx_data</code>, <code>o_axi_rx_valid</code>, <code>o_axi_rx_keep</code>, and <code>o_axi_rx_last</code> form the downstream receive stream.</li> <li><code>o_frame_start</code> pulses when a new frame is accepted; <code>Top.v</code> uses it to timestamp ingress.</li> </ul>"},{"location":"FPGA/MAC_layer_rx/#start-and-end-detection","title":"Start and End Detection","text":"<ol> <li>Start indication can only be at zeroth or fourth place (from left) in <code>i_xgmii_rxc</code>.</li> <li>End indication can be any bit in <code>i_xgmii_rxc</code>.</li> <li>They are using combinational logic, so once there is valid bit in <code>i_xgmii_rxc</code>, the corresponding sof or eof bit will be asserted at the same time.</li> </ol> <pre><code>    genvar      idx;\n    generate\n        for (idx = 0; idx &lt; 2; idx = idx + 1 ) begin:sof_check\n            assign sof_location [idx] = i_xgmii_rxc[idx*4] &amp;&amp; (i_xgmii_rxd[idx*32 +:8] == XGMII_START);\n        end\n    endgenerate\n    generate\n        for (idx = 0; idx &lt; 8; idx = idx + 1 ) begin:eof_check\n            assign eof_location [idx] = i_xgmii_rxc[idx] &amp;&amp; (i_xgmii_rxd[idx*8 +:8] == XGMII_TERM);\n        end\n    endgenerate\n</code></pre> <p><code>sof_location[0]</code> means the frame starts in byte 0 of the current word. <code>sof_location[1]</code> means the frame starts in byte 4, so the payload must be stitched across two cycles. Below is the illustration of possible sof and eof location in xgmii_rxc.</p> <p></p>"},{"location":"FPGA/MAC_layer_rx/#state-machine","title":"State Machine","text":"<p>There are three states in <code>cur_state</code></p> <ul> <li><code>IDLE</code>: <ol> <li>waiting for start of the frame. Once any of <code>sof_location</code> bit is true (|sof_location), it changes to <code>SOF</code> state.</li> <li>Assert <code>o_frame_start</code>. This is used to start <code>time_stamp</code>.</li> </ol> </li> <li> <p><code>SOF</code>:</p> <ol> <li>It means the start of the frame, as well as frame transmitting.</li> <li><code>valid_d1</code> will be asserted, which is the indication of the payload. As it is a register, there is one cycle  latency for it to be true asserted. This is done on purpose which is used to discard the preamble and SFD .</li> <li>It checks <code>|eof_location</code>, which is an indication of end of the frame.</li> <li>It assign <code>eof_location</code> to <code>eof_reg</code>. This is used for the proper <code>last</code> and <code>keep</code> signal in AXI channel.</li> </ol> </li> <li> <p><code>TERM</code>: </p> <ol> <li>desert <code>valid_1</code>.</li> <li>return to <code>IDLE</code>.</li> </ol> </li> </ul>"},{"location":"FPGA/MAC_layer_rx/#o_axi_rx_valid-signal","title":"<code>o_axi_rx_valid</code> signal","text":"<ol> <li> <p>Assertion</p> <p>When <code>cur_state</code> is <code>SOF</code>. directly assign <code>valid_d1</code> to it. </p> </li> <li> <p>De-assertion</p> <p>It depends on sof and eof. Even when <code>cur_state</code> is <code>EOF</code>, it does not mean we can de-assert it. Some combination of sof and eof can make <code>o_axi_rx_valid</code> last for one more cycle.</p> </li> </ol>"},{"location":"FPGA/MAC_layer_rx/#data-parsing","title":"Data Parsing","text":"<ol> <li> <p>if <code>sof_reg</code> is <code>2'b00</code></p> <p>It means no data.</p> </li> <li> <p>if <code>sof_reg</code> is <code>2'b01</code></p> <p>It means the data is always <code>i_xgmii_rxd_1</code></p> </li> <li> <p>if <code>sof_reg</code> is <code>2'b10</code></p> <p>It means the when we detecting the sof, half of the data is already there. Thus, we should make a stitch like <code>{current_rxd,previous_rxd}</code>, where previous_rxd is <code>data_saver</code>. Below shows how data is stitched.</p> </li> </ol> <p></p>"},{"location":"FPGA/MAC_layer_rx/#o_axi_rx_last-signal","title":"<code>o_axi_rx_last</code> signal","text":"<p>It depends on <code>cur_state</code>, <code>sof_reg</code> and <code>eof_reg</code>. To be honest, it should not be asserted in the first few cycles in practice, as there is always a minimal length for an Ethernet frame. I made the assertion of it possible in <code>SOF</code> just to align with the algorithm. Just keep in mind that depending on combination of <code>sof_reg</code> and <code>eof_reg</code>, the last signal can be asserted at <code>TERM</code> or before <code>TERM</code>, as there is nothing left for transmit.</p>"},{"location":"FPGA/MAC_layer_rx/#o_axi_rx_keep-signal","title":"<code>o_axi_rx_keep</code> signal","text":"<p>Here we used enumination, to list all the possible combination. A simple math can manage.</p>"},{"location":"FPGA/MAC_layer_rx/#design-limits","title":"Design Limits","text":"<ul> <li>No CRC check: bad FCS is not detected here.</li> <li>CRC is preserved: downstream logic must ignore or strip it if needed.</li> <li>No buffering on stall: <code>!i_axi_rx_ready</code> resets the receive state and abandons the current frame.</li> <li>64-bit specific: the logic assumes <code>DATA_WIDTH=64</code> and <code>CTRL_WIDTH=8</code>, even though they are written as parameters.</li> </ul>"},{"location":"FPGA/arbiter/","title":"arbiter","text":"<p>This module selects which symbol_book is allowed to output its next top-of-book event. It applies a round-robin policy across all symbol FIFOs so one active symbol does not permanently block the others.</p>"},{"location":"FPGA/arbiter/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/order_book_related/dependencies/arbiter.v</code>. It is instantiated in order_book_builder as <code>event_fifo_arbiter_inst</code>.</p> <p>Each <code>symbol_book</code> has its own event FIFO. The arbiter looks at all of them in parallel and chooses one source to pop in the current cycle.</p>"},{"location":"FPGA/arbiter/#design-logic","title":"Design Logic","text":"<p>The arbiter solves a scheduling problem:</p> <ol> <li>many <code>symbol_book</code> instances can have pending events at the same time</li> <li>only one payload should be forwarded to the shared output in one cycle</li> <li>the selected source should rotate fairly over time</li> </ol> <p>To do that, the module keeps a round-robin pointer <code>rr_pointer</code>. Each cycle, it scans the <code>i_src_not_empty</code> bitmap starting from that pointer and picks the first non-empty source it finds. After choosing one source, it advances the round-robin pointer to the next index after the chosen one.</p> <p>This gives a simple fairness rule: after one source is served, the next search starts from the following source instead of always starting from source <code>0</code>.</p>"},{"location":"FPGA/arbiter/#interface","title":"Interface","text":""},{"location":"FPGA/arbiter/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code>: main clock.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_src_not_empty</code>: one bit per source FIFO, indicating that the source has at least one queued event.</li> <li><code>i_src_valid</code>: one bit per source FIFO, indicating that the source is returning valid pop data.</li> <li><code>i_src_payload</code>: concatenated payload bus from all sources.</li> </ul>"},{"location":"FPGA/arbiter/#outputs","title":"Outputs","text":"<ul> <li><code>o_src_pop</code>: one-hot pop request back to the selected source.</li> <li><code>o_valid</code>: valid flag for the selected output payload.</li> <li><code>o_payload</code>: payload from the selected source.</li> </ul>"},{"location":"FPGA/arbiter/#round-robin-scan","title":"Round-Robin Scan","text":"<p>The combinational scan uses:</p> <ul> <li><code>rr_pointer</code>: where the search starts</li> <li><code>scan_idx</code>: current scanned source index</li> <li><code>pointer</code>: winning source index for this cycle</li> <li><code>found_nonempty</code>: whether the scan already found one eligible source</li> </ul> <p>The helper function <code>wrap_idx()</code> implements circular indexing so the search can move past the last source and wrap back to <code>0</code>.</p> <p>Example with <code>SYMBOL_NUM = 4</code> and <code>rr_pointer = 2</code>:</p> <ul> <li>scan order is <code>2 -&gt; 3 -&gt; 0 -&gt; 1</code></li> <li>if source <code>3</code> is the first non-empty entry in that order, it wins</li> <li>the next <code>rr_pointer</code> becomes <code>0</code></li> </ul>"},{"location":"FPGA/arbiter/#pop-generation","title":"Pop Generation","text":"<p>The output <code>o_src_pop</code> is generated as a one-hot vector:</p> <pre><code>assign o_src_pop[src_idx] = found_nonempty &amp;&amp; (pointer == src_idx);\n</code></pre> <p>So only the chosen source is asked to pop in that cycle.</p>"},{"location":"FPGA/arbiter/#payload-selection","title":"Payload Selection","text":"<p>One subtle part of the design is that <code>o_valid</code> and <code>o_payload</code> are indexed by <code>latch_pointer</code>, not by the current combinational <code>pointer</code>:</p> <pre><code>assign o_valid   = i_src_valid[latch_pointer];\nassign o_payload = i_src_payload[latch_pointer*PAYLOAD_W +: PAYLOAD_W];\n</code></pre> <p>This is needed because the source FIFO payload and valid bit are observed after the selected pop request path. The module first decides which source to pop, then latches that source index into <code>latch_pointer</code>, and uses the latched index to select the returned payload.</p> <p>So:</p> <ol> <li>combinational logic chooses which FIFO to pop now</li> <li>sequential logic stores that choice in <code>latch_pointer</code></li> <li>output muxing uses <code>latch_pointer</code> to forward the selected payload</li> </ol>"},{"location":"FPGA/arbiter/#pointer-update","title":"Pointer Update","text":"<p>On each cycle where a non-empty source is found:</p> <ul> <li><code>latch_pointer &lt;= pointer</code></li> <li><code>rr_pointer &lt;= pointer + 1</code>, with wraparound</li> </ul> <p>If the selected source is the last index, <code>rr_pointer</code> wraps back to zero.</p> <p>If no source is non-empty, the pointers hold their previous values.</p>"},{"location":"FPGA/arbiter/#reset-behavior","title":"Reset Behavior","text":"<p>On reset, both pointers are cleared:</p> <ul> <li><code>rr_pointer = 0</code></li> <li><code>latch_pointer = 0</code></li> </ul> <p>So after reset, the first scan starts from source <code>0</code>.</p>"},{"location":"FPGA/arbiter/#how-it-is-used-in-order_book_builder","title":"How It Is Used In order_book_builder","text":"<p>In order_book_builder, each <code>symbol_book</code> exports:</p> <ul> <li><code>o_not_empty</code></li> <li><code>o_valid</code></li> <li><code>o_payload</code></li> </ul> <p>These are packed into:</p> <ul> <li><code>arb_src_not_empty</code></li> <li><code>arb_src_valid</code></li> <li><code>arb_src_payload</code></li> </ul> <p>The arbiter then decides which symbol FIFO to pop and forwards that symbol's top-of-book snapshot onto the shared <code>o_payload</code> output of <code>order_book_builder</code>.</p>"},{"location":"FPGA/arbiter/#limits","title":"Limits","text":"<ul> <li>The arbiter uses a linear scan over all <code>SYMBOL_NUM</code> sources each cycle, so the combinational path grows with the number of symbols.</li> <li>Fairness is round-robin among non-empty sources, but it does not account for queue depth or age beyond that order.</li> <li>The module assumes each source presents <code>i_src_valid</code> and <code>i_src_payload</code> in a way that matches the pop-and-latch timing used here.</li> </ul>"},{"location":"FPGA/async_fifo/","title":"async_fifo","text":"<p>This module is an asynchronous FIFO used to pass event payloads between two clock domains. In this project, it transfers <code>order_book_builder</code> events from the receive clock domain into the PCIe/control-plane clock domain.</p>"},{"location":"FPGA/async_fifo/#introduction","title":"Introduction","text":"<p>The implementation is in <code>verilog_src/async_fifo.v</code>. In <code>verilog_src/Top.v</code>, it is instantiated as <code>event_cdc_fifo_inst</code>.</p> <p>Its write side runs on <code>i_wr_clk</code>, and its read side runs on <code>i_rd_clk</code>. That is the key difference from the ordinary fifo: this module is specifically designed for clock-domain crossing.</p>"},{"location":"FPGA/async_fifo/#design-logic","title":"Design Logic","text":"<p>The main problem is that the write side and read side do not share one clock. A normal FIFO pointer comparison would not be safe across domains.</p> <p>This design solves that by using:</p> <ol> <li>binary read and write pointers for local memory indexing</li> <li>Gray-coded copies of those pointers for clock-domain crossing</li> <li>two-flop synchronizers for the Gray-coded pointers</li> <li>local full and empty checks using the synchronized remote pointer</li> </ol> <p>The important idea is that only the Gray-coded pointer crosses the clock domain. Gray code changes only one bit at a time, which reduces ambiguity when the other clock domain samples it asynchronously.</p>"},{"location":"FPGA/async_fifo/#interface","title":"Interface","text":""},{"location":"FPGA/async_fifo/#inputs","title":"Inputs","text":"<ul> <li><code>i_wr_clk</code>: write clock.</li> <li><code>i_wr_rst</code>: write-domain reset.</li> <li><code>i_wr_en</code>: write request.</li> <li><code>i_wr_data</code>: write payload.</li> <li><code>i_rd_clk</code>: read clock.</li> <li><code>i_rd_rst</code>: read-domain reset.</li> <li><code>i_rd_en</code>: read request.</li> </ul>"},{"location":"FPGA/async_fifo/#outputs","title":"Outputs","text":"<ul> <li><code>o_wr_full</code>: write-side full flag.</li> <li><code>o_rd_empty</code>: read-side empty flag.</li> <li><code>o_rd_valid</code>: read-data valid pulse.</li> <li><code>o_rd_data</code>: read payload.</li> </ul>"},{"location":"FPGA/async_fifo/#storage","title":"Storage","text":"<p>The FIFO storage is:</p> <pre><code>reg [DATA_W-1:0] mem [0:DEPTH-1];\n</code></pre> <p>The module keeps two versions of each pointer:</p> <ul> <li><code>wr_ptr_bin</code> and <code>rd_ptr_bin</code>: binary pointers used for RAM indexing</li> <li><code>wr_ptr_gray</code> and <code>rd_ptr_gray</code>: Gray-coded pointers used for CDC</li> </ul> <p>The local parameter <code>PTR_W = ADDR_W + 1</code> adds one extra bit so the full and empty logic can distinguish pointer wraparound.</p>"},{"location":"FPGA/async_fifo/#write-side","title":"Write Side","text":"<p>The write-domain action is controlled by:</p> <pre><code>wire wr_do = i_wr_en &amp;&amp; !o_wr_full;\n</code></pre> <p>If <code>wr_do</code> is true on <code>i_wr_clk</code>:</p> <ul> <li>the payload is written into <code>mem[wr_ptr_bin[ADDR_W-1:0]]</code></li> <li>the write pointer advances</li> <li>the Gray-coded write pointer is updated</li> </ul> <p>The write side also synchronizes the read pointer Gray code:</p> <ul> <li><code>rd_ptr_gray_sync1</code></li> <li><code>rd_ptr_gray_sync2</code></li> </ul> <p>Those two registers are marked with <code>(* ASYNC_REG = \"TRUE\" *)</code> so the synthesis and implementation tools treat them as CDC synchronizers.</p>"},{"location":"FPGA/async_fifo/#read-side","title":"Read Side","text":"<p>The read-domain action is controlled by:</p> <pre><code>wire rd_do = i_rd_en &amp;&amp; !o_rd_empty;\n</code></pre> <p>If <code>rd_do</code> is true on <code>i_rd_clk</code>:</p> <ul> <li><code>o_rd_data &lt;= mem[rd_ptr_bin[ADDR_W-1:0]]</code></li> <li>the read pointer advances</li> <li><code>o_rd_valid &lt;= 1</code></li> </ul> <p>If no read happens, <code>o_rd_valid</code> is cleared back to <code>0</code>.</p> <p>The read side also synchronizes the write pointer Gray code:</p> <ul> <li><code>wr_ptr_gray_sync1</code></li> <li><code>wr_ptr_gray_sync2</code></li> </ul>"},{"location":"FPGA/async_fifo/#gray-code-logic","title":"Gray Code Logic","text":"<p>The next-pointer logic is:</p> <pre><code>wr_gray_next = (wr_bin_next &gt;&gt; 1) ^ wr_bin_next;\nrd_gray_next = (rd_bin_next &gt;&gt; 1) ^ rd_bin_next;\n</code></pre> <p>This is the standard binary-to-Gray conversion.</p> <p>Because only one bit changes between adjacent Gray-code values, the synchronized pointer is less likely to be interpreted incorrectly when sampled by the other clock domain.</p>"},{"location":"FPGA/async_fifo/#empty-and-full-detection","title":"Empty And Full Detection","text":"<p>The read side considers the FIFO empty when the next read pointer would match the synchronized write pointer:</p> <pre><code>rd_empty_next = (rd_gray_next == wr_ptr_gray_sync2);\n</code></pre> <p>The write side considers the FIFO full when the next write pointer would match the synchronized read pointer in the standard async-FIFO full form:</p> <pre><code>wr_full_cmp  = {~rd_ptr_gray_sync2[PTR_W-1:PTR_W-2], rd_ptr_gray_sync2[PTR_W-3:0]};\nwr_full_next = (wr_gray_next == wr_full_cmp);\n</code></pre> <p>That comparison detects the case where the write pointer is about to lap the read pointer by one full buffer.</p>"},{"location":"FPGA/async_fifo/#reset-behavior","title":"Reset Behavior","text":"<p>On write reset:</p> <ul> <li><code>wr_ptr_bin = 0</code></li> <li><code>wr_ptr_gray = 0</code></li> <li>synchronized read-pointer copies are cleared</li> <li><code>o_wr_full = 0</code></li> </ul> <p>On read reset:</p> <ul> <li><code>rd_ptr_bin = 0</code></li> <li><code>rd_ptr_gray = 0</code></li> <li>synchronized write-pointer copies are cleared</li> <li><code>o_rd_empty = 1</code></li> <li><code>o_rd_valid = 0</code></li> <li><code>o_rd_data = 0</code></li> </ul> <p>So after reset, the FIFO starts empty.</p>"},{"location":"FPGA/async_fifo/#how-it-is-used-in-top","title":"How It Is Used In Top","text":"<p>In <code>verilog_src/Top.v</code>, the FIFO bridges:</p> <ul> <li>write domain: <code>w_xgmii_rx_clk</code></li> <li>read domain: <code>user_clk_250</code></li> </ul> <p>The write payload is <code>builder_event_payload</code>, and the write enable is <code>builder_event_valid</code>. The read side is driven with <code>i_rd_en = 1'b1</code>, so whenever the FIFO is non-empty, events are drained into <code>event_payload</code> with <code>event_valid</code>.</p>"},{"location":"FPGA/async_fifo/#why-async-fifo-is-needed","title":"Why Async FIFO Is Needed","text":"<p><code>order_book_builder</code> runs in the receive-clock domain, while downstream logic that consumes events runs in a different clock domain. A direct wire connection would risk metastability and payload corruption.</p> <p>The async FIFO solves that by:</p> <ul> <li>storing payloads in RAM</li> <li>synchronizing only the Gray-coded pointers across domains</li> <li>letting each side operate using its own local clock</li> </ul>"},{"location":"FPGA/async_fifo/#limits","title":"Limits","text":"<ul> <li>The implementation assumes the classic power-of-two async-FIFO pointer scheme; <code>DEPTH</code> should be chosen carefully to match that structure.</li> <li><code>o_rd_valid</code> only pulses when an actual read occurs; it is not a persistent data-valid flag.</li> <li>The memory itself is not dual-clock protected by hand-written arbitration logic; correctness relies on the standard async-FIFO pointer discipline.</li> <li>There is no almost-full or almost-empty indication.</li> </ul>"},{"location":"FPGA/order_book_parser/","title":"order_book_parser","text":"<p>Parse Ethernet/IPv4/UDP payload data from MAC_layer_rx into a compact event interface. The module first walks fixed packet headers, then peeks message count and message type, and finally extracts fields such as <code>stock_locate</code>, <code>order_ref_num</code>, <code>shares</code>, and <code>price</code>.</p>"},{"location":"FPGA/order_book_parser/#design-logic","title":"Design Logic","text":""},{"location":"FPGA/order_book_parser/#the-header","title":"The header","text":"<p>Before pasing the messages, we have to deal with Ethernet head, IP header and UDP/TCP header (The preabmle+SFD has already been filtered out in MAC layer), as illustrated below:</p> <p></p> <p>The lengths of headers are always fixed. The parsing of them is easy. We use a counter (<code>head_counter</code>) to indicate the end of headers.</p>"},{"location":"FPGA/order_book_parser/#the-messages","title":"The messages","text":"<p>For each message, parsing is not finised until we hit the end from <code>i_axi_rx_data</code>. Thus, we have to wait for it (use a buffer). However, The end of the previous message and the start of the next message may be mixed in one <code>i_axi_rx_data</code>. So, we need another counter (<code>buffed_bytes</code>).</p>"},{"location":"FPGA/order_book_parser/#states","title":"States","text":"<p>In the big picture, as stated above, the parsing can be divided into two phases:</p> <ol> <li>header parsing.</li> <li>message parsing.</li> </ol> <p>which is shown below:</p> <p></p> <ol> <li>At the begining, header parsing will be kicked off when <code>i_axi_rx_valid == 1</code>, from which <code>head_counter</code> starts to <code>+1</code>. </li> <li>When it reaches <code>6</code>, it kicks off the message parsing phase, It gets <code>msg_count</code> and start to buffer. </li> <li>Every time when <code>msg_count !=1</code>, it peeks the type and wait until the buffer is enough. </li> <li>Once it parsed the message, <code>msg_count &lt;= msg_count -1;</code> and <code>buffered_bytes</code> will also be subtracted corresponding length.  </li> <li>The peak type -&gt; data to buffer -&gt; parse loop will be exectued again, until <code>msg_count == 0</code>. </li> <li>For unknown types, the <code>default</code> case will do nothing except subtract <code>msg_count</code> and <code>buffered_bytes</code>.</li> </ol>"},{"location":"FPGA/order_book_parser/#control-from-the-host","title":"Control from the host","text":"<p>I want to add control like filtering out frames sent to other IP or MACs, realized by host sending register control signals through PCIe. However, this has nothing to do with the parsing itself, so now it is suspended. Will be realized in the future.</p>"},{"location":"FPGA/order_book_parser/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_parser/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code> is the parser clock.</li> <li><code>i_rst</code> clears the header parser, message parser, and output registers.</li> <li><code>i_axi_rx_data</code>, <code>i_axi_rx_valid</code>, <code>i_axi_rx_keep</code>, <code>i_axi_rx_last</code>, and <code>i_axi_rx_ingress_tick</code> come from <code>MAC_layer_rx</code>.</li> <li><code>i_ctl_dst_mac</code>, <code>i_ctl_dst_ip</code>, <code>i_ctl_dst_port</code>, <code>i_promiscuous</code>, <code>i_sync_fire</code>, and <code>i_ctl_reg</code> program the parser settings registers.</li> </ul>"},{"location":"FPGA/order_book_parser/#outputs","title":"Outputs","text":"<ul> <li><code>o_axi_rx_ready</code> is tied high, so this block always accepts incoming beats.</li> <li><code>o_msg_valid</code> pulses when one complete message has been decoded.</li> <li><code>o_seq_num</code> and <code>o_rx_ingress_tick</code> carry packet-level context.</li> <li><code>o_msg_type</code>, <code>o_stock_locate</code>, <code>o_order_ref_num</code>, <code>o_new_order_ref_num</code>, <code>o_buy_sell</code>, <code>o_shares</code>, <code>o_price</code>, and <code>o_timestamp</code> carry the parsed order-book fields.</li> </ul>"},{"location":"FPGA/order_book_parser/#sliding-buffer","title":"Sliding Buffer","text":"<p>The module keeps a 512-bit (64 bytes) history window so fields can cross AXI beat boundaries without special cases.</p> <pre><code>reg  [511:0] prev_buff;\nwire [511:0] cur_buff = {prev_buff[447:0], i_axi_rx_data};\nwire [6:0] valid_bytes = buffed_bytes &lt; 7'd6 ? 7'd0 : buffed_bytes - 7'd6;\n</code></pre> <p><code>prev_buff</code> is sequential state. <code>cur_buff</code> mixes the previous window with the current beat, which lets the parser read the next field one cycle earlier. <code>valid_bytes</code> accounts for the fixed 6-byte gap between the current parse boundary and the start of the valid window.</p>"},{"location":"FPGA/order_book_parser/#state-machine","title":"State Machine","text":"<p>The body parser is a 5-state machine.</p> <ul> <li> <p><code>IDLE</code>:</p> <ol> <li>Clear parser-local state such as <code>prev_buff</code>, <code>o_msg_valid</code>, <code>buffed_bytes</code>, <code>o_msg_type</code>, and <code>msg_count</code>.</li> <li>Wait until <code>head_counter == 6</code>, then move to <code>PEEKING_MSG_COUNT</code>.</li> </ol> </li> <li> <p><code>PEEKING_MSG_COUNT</code>:</p> <ol> <li>Wait for <code>i_axi_rx_valid</code>.</li> <li>Latch the packet message count from <code>i_axi_rx_data[31:16]</code>.</li> <li>Shift the current AXI beat into <code>prev_buff</code>, increment <code>buffed_bytes</code>, and move to <code>PEEKING_TYPE</code>.</li> </ol> </li> <li> <p><code>PEEKING_TYPE</code>:</p> <ol> <li>Clear the previous decoded message outputs.</li> <li>If <code>msg_count == 0</code>, return to <code>IDLE</code>.</li> <li>Otherwise, wait for <code>i_axi_rx_valid</code>, peek the next message type with <code>take_data(cur_buff, valid_bytes-2, 1)</code>, shift in the new beat, and move to <code>PARSING_BODY</code>.</li> </ol> </li> <li> <p><code>PARSING_BODY</code>:</p> <ol> <li>If <code>buffed_bytes &gt;= 64</code>, enter <code>ERROR</code>.</li> <li>If <code>valid_bytes</code> is large enough for <code>msg_len_bytes(peeked_type)</code>, decode one full message, decrement <code>msg_count</code>, and return to <code>PEEKING_TYPE</code>.</li> <li>Otherwise, keep shifting AXI beats into the sliding buffer until enough bytes are available.</li> </ol> </li> <li> <p><code>ERROR</code>:</p> <ol> <li>Stop decoding and hold state until <code>i_axi_rx_last</code> arrives.</li> <li>Return to <code>IDLE</code> at the end of the packet.</li> </ol> </li> <li> <p><code>PARSING_HEADER</code>:</p> <ol> <li>This state is declared in the RTL but is not used by the current transition logic.</li> </ol> </li> </ul>"},{"location":"FPGA/order_book_parser/#supported-message-types","title":"Supported Message Types","text":"<p>The parser recognizes seven message types and uses <code>msg_len_bytes()</code> to decide when a full message is present.</p> <ul> <li> <p><code>A</code> (<code>7'd38</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, <code>buy_sell</code>, <code>shares</code>, and <code>price</code>.</li> <li>Skips tracking number and stock symbol.</li> </ol> </li> <li> <p><code>X</code> (<code>7'd25</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, and canceled <code>shares</code>.</li> </ol> </li> <li> <p><code>D</code> (<code>7'd21</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, and <code>order_ref_num</code>.</li> </ol> </li> <li> <p><code>U</code> (<code>7'd37</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, original <code>order_ref_num</code>, <code>new_order_ref_num</code>, <code>shares</code>, and <code>price</code>.</li> </ol> </li> <li> <p><code>E</code> (<code>7'd33</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, and executed <code>shares</code>.</li> <li>Skips match number.</li> </ol> </li> <li> <p><code>F</code> (<code>7'd42</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, <code>buy_sell</code>, <code>shares</code>, and <code>price</code>.</li> <li>Skips stock symbol and attribution.</li> </ol> </li> <li> <p><code>C</code> (<code>7'd38</code>):</p> <ol> <li>Outputs <code>stock_locate</code>, <code>timestamp</code>, <code>order_ref_num</code>, <code>shares</code>, and <code>price</code>.</li> <li>Skips match number, printable flag, and attribution.</li> </ol> </li> <li> <p>Shared note:</p> <ol> <li>Fields such as tracking number are consumed for alignment but are not exposed at the output interface.</li> </ol> </li> </ul>"},{"location":"FPGA/order_book_parser/#field-extraction","title":"Field Extraction","text":"<p><code>take_data()</code> reads a field from <code>cur_buff</code> by byte index and width, then preserves the original byte order in the output register.</p> <pre><code>take_data[(width_bytes-i)*8-1 -: 8] =\n    prev_buff[(index_bytes-i)*8-1 -: 8];\n</code></pre> <p>This helper is the reason each message case can describe fields in byte offsets instead of manually slicing every possible boundary crossing.</p>"},{"location":"FPGA/order_book_parser/#waveform-snapshot","title":"Waveform snapshot","text":"<p>Below shows an example of the waveform, in which the orders are parsed by this module. The pink waves are axi data comming from MAC_layer_rx. It can be noted after several cycles (the headers), the messages are parsed out, indicated by <code>o_msg_valid</code>. There are also three examples of message <code>TYPE_A</code>, <code>TYPE_F</code> and <code>TYPE_U</code>.</p> <p></p>"},{"location":"FPGA/order_book_parser/#design-limits","title":"Design Limits","text":"<ul> <li><code>o_axi_rx_ready</code> is always <code>1'b1</code>, so this module provides no receive backpressure.</li> <li><code>i_axi_rx_keep</code> is present on the interface but is not used in the current parser logic.</li> <li>The programmable destination filters are stored but not applied to gate parsing.</li> <li>If <code>buffed_bytes &gt;= 64</code>, the parser enters <code>ERROR</code> and waits for <code>i_axi_rx_last</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/overview/","title":"order_book_builder","text":"<p>This module receives the messages from order_book_parser. In side this module, for each configured symbol, it constructs 1. an order book and 2. a bid/ask level book for it. It always outputs the best bid/ask price and the corresponding shares of the symbol.</p> <p>Inside, there are these modules:</p> <ol> <li>symbol_book x N.</li> <li>arbiter</li> </ol>"},{"location":"FPGA/order_book_builder/overview/#hierarchy","title":"Hierarchy","text":"<p>Here gives a simply hierarchy of order_book_builder. When parsed messages are input from order_book_parser, it will be fanned out to all the symbol_books. Inside, symbol_book will check if the symbol is the configured one and discard the unmatches. Best bid and ask, along with the shares, will be stored in a fifo inside symbol_book. Round-robin is applied in the arbiter. It will decide which symbol_book's best bid/ask will be output. </p> <p></p>"},{"location":"FPGA/order_book_builder/overview/#features","title":"Features","text":"<ul> <li>Flexibility: Multiple symbol order book building can be done by instantiating multiple symbol_book instances.</li> <li>Per-symbol event routing by <code>stock_locate</code>.</li> <li>Round-robin arbitration into one output stream.</li> </ul>"},{"location":"FPGA/order_book_builder/overview/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/overview/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code> is the main clock shared with <code>order_book_parser</code> and the downstream symbol books.</li> <li><code>i_rst</code> is an active-high reset for the wrapper, all <code>symbol_book</code> instances, and the arbiter.</li> <li><code>i_msg_valid</code> marks one decoded parser event as valid.</li> <li><code>i_seq_num</code> carries the sequence number attached to the parser event.</li> <li><code>i_rx_ingress_tick</code> carries the packet ingress tick from the receive path. (not used now)</li> <li><code>i_msg_type</code> identifies the ITCH message type, such as <code>A</code>, <code>D</code>, <code>X</code>, <code>U</code>, <code>E</code>, or <code>F</code>.zhege</li> <li><code>i_stock_locate</code> selects which per-symbol book should consume the event.</li> <li><code>i_order_ref_num</code> carries the original order reference number.</li> <li><code>i_new_order_ref_num</code> is used by replace messages such as <code>U</code>.</li> <li><code>i_buy_sell</code> is the ASCII side field from the parser, typically <code>'B'</code> or <code>'S'</code>.</li> <li><code>i_shares</code> carries the event quantity.</li> <li><code>i_price</code> carries the event price.</li> <li><code>i_timestamp</code> carries the parser timestamp (not used now).</li> </ul>"},{"location":"FPGA/order_book_builder/overview/#outputs","title":"Outputs","text":"<ul> <li><code>o_valid</code> pulses when the arbiter emits one top-of-book snapshot from any <code>symbol_book</code>.</li> <li><code>o_payload</code> is a <code>274-bit</code> packed snapshot with this layout:<ul> <li><code>o_payload[273]</code>:       ask best valid</li> <li><code>o_payload[272:241]</code>:   ask best price</li> <li><code>o_payload[240:209]</code>:   ask best shares</li> <li><code>o_payload[208:145]</code>:   ask sequence number</li> <li><code>o_payload[144]</code>:       bid best valid</li> <li><code>o_payload[143:112]</code>:   bid best price</li> <li><code>o_payload[111:80]</code>:    bid best shares</li> <li><code>o_payload[79:16]</code>:     bid sequence number</li> <li><code>o_payload[15:0]</code>:      stock locate</li> </ul> </li> </ul>"},{"location":"FPGA/order_book_builder/overview/#waveform-snapshot","title":"Waveform snapshot","text":"<p>Below gives the snapshot of the waveform. </p> <ol> <li>At time 1 (inside the white circle), parsed message arrived. <code>66</code> in <code>o_buy_sell</code> means buy. Thus, we add price <code>100</code> (actually 0.01 USD) with shares <code>100</code> to the bid side. The light blue waves indicate the change. </li> <li>At time 2, there is another buy signal, price is <code>200</code> while shares are <code>500</code>. The output best bid and the corresponding shares become to <code>200</code> and <code>500</code>, respectively. </li> <li>At time 3, The signal type is 88 in decimal, which means cancel. It cancels the price <code>100</code> bid order. In this case the best bid and the shares remain the same. </li> <li>From time 4 to 5, the shares of <code>200</code> is reducing to <code>0</code>. </li> <li>At time 6, there is a new bid order with price <code>300</code> and shares <code>20</code>, the output best bid and shares change correspondingly.</li> </ol> <p>The ask side behaves the same if there are ask signals. Note that the best price and shares signals (like <code>o_bid_best_valid</code>, etc.) will be all packed to <code>o_payload</code>. There signals here are just for demonstration while won't be presented in the module.</p> <p>Figure here is a little bit small. Zoom in the page to check.</p> <p></p>"},{"location":"FPGA/order_book_builder/overview/#limits","title":"Limits","text":"<ul> <li><code>i_rx_ingress_tick</code> and <code>i_timestamp</code> are not used now.</li> <li>The output payload contains only best bids and best ask, not the full depth or raw parser event.</li> <li>This wrapper exposes only <code>o_valid</code> and <code>o_payload</code>; it does not provide an output ready/handshake signal.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/","title":"fifo","text":"<p>This module is the event queue used inside symbol_book. It stores the latest top-of-book snapshots generated by one symbol so the external arbiter can pop them later.</p>"},{"location":"FPGA/order_book_builder/symbol_book/fifo/#introduction","title":"Introduction","text":"<p>In symbol_book, this FIFO is instantiated as <code>event_fifo_inst</code>.</p> <p>Its role is straightforward:</p> <ol> <li>one <code>symbol_book</code> generates snapshot events when its best bid or ask changes</li> <li>the snapshot is pushed into this FIFO</li> <li>the global arbiter decides when to pop that symbol's next event</li> </ol> <p>So the FIFO decouples event generation from round-robin output scheduling.</p>"},{"location":"FPGA/order_book_builder/symbol_book/overview/","title":"symbol_book","text":"<p>This module is the key to realize multiple symbol order book building. Each <code>symbol_book</code> only manages one symbol. Inside, it contains:</p> <ol> <li> <p>one book_builder</p> <ul> <li>It maintains a live order book inside. It also sends operations and data to ask_wrapper_inst and bid_wrapper_inst to guide them how to operate the price level book.</li> <li>The <code>stock_locate</code> signal in the incoming message will be checked before it inputs to book_builder. Unmatched messages will be discarded.</li> </ul> </li> <li> <p>one ask_wrapper_inst</p> <ul> <li>It maintains a live price level book on ask side. It also has a tree structure, which constantly updates the best price now. It outputs the best ask and the corresponding shares to the fifo once there is an event.</li> </ul> </li> <li> <p>one bid_wrapper_inst</p> <ul> <li>Bid version of above</li> </ul> </li> <li> <p>one event_fifo_inst</p> <ul> <li>As there can be multiple <code>symbol_book</code>s in one order_book_builder, an arbiter is used to schedule the output of <code>symbol_book</code>s. The output of <code>symbol_book</code> will be saved in to their event_fifo_inst first, arbiter decides which <code>symbol_book</code> gets popped out.</li> </ul> </li> </ol>"},{"location":"FPGA/order_book_builder/symbol_book/overview/#hierarchy","title":"Hierarchy","text":"<p>symbol_book is a little be complex, below gives the inside structure for better understanding.</p> <p></p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/","title":"bram","text":"<p>This module is the single-port storage used by book_builder as <code>order_book_inst</code>. It keeps the current live order state for one symbol, so later cancel, execute, delete, or replace messages can find the old side, shares, and price.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#introduction","title":"Introduction","text":"<p>In <code>book_builder</code>, it is instantiated with <code>ADDR_WIDTH = BOOK_LEVEL_BIT</code> and <code>DATA_WIDTH = 67</code>, so each entry stores <code>{valid, side, shares, price}</code> for one hashed order reference.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#design-logic","title":"Design Logic","text":"<p>The module is intentionally small:</p> <p>BRAM is a dedicated on-chip memory block provided by the FPGA fabric, separate from ordinary flip-flop registers and LUTs. It is used here because the order table is naturally a memory array: with <code>BOOK_LEVEL_BIT = 12</code>, the design needs <code>4096</code> (can adjust) addressable rows, and each row stores <code>67</code> bits of live-order state. Implementing that table with BRAM is much more area-efficient than building the same storage from registers.</p> <p>The tradeoff is that read and write are clocked operations. In this module, a read result appears on <code>o_data</code> after the next rising edge, so book_builder uses the <code>FIRST_CYCLE</code> and <code>SECOND_CYCLE</code> states before it consumes <code>book_o_data</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code>: BRAM clock.</li> <li><code>i_rst</code>: clears only the output register <code>o_data</code>.</li> <li><code>i_addr</code>: row address.</li> <li><code>i_op</code>: operation selector, where <code>2'b01</code> is read and <code>2'b10</code> is write.</li> <li><code>i_data</code>: row payload to write.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#outputs","title":"Outputs","text":"<ul> <li><code>o_data</code>: registered read data.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#stored-format","title":"Stored Format","text":"<p>Inside book_builder, the row width is <code>67</code> bits:</p> <pre><code>{valid, side[1:0], shares[31:0], price[31:0]}\n</code></pre> <p>Example:</p> <pre><code>{1'b1, 2'b01, 32'd100, 32'd200}\n</code></pre> <p>This means the slot is valid, the order is on the bid side, the resting quantity is <code>100</code>, and the price is <code>200</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#read-and-write-behavior","title":"Read And Write Behavior","text":"<ul> <li><code>WRITE</code>: on the rising edge, <code>bram[i_addr] &lt;= i_data</code>.</li> <li><code>READ</code>: on the rising edge, <code>o_data &lt;= bram[i_addr]</code>.</li> <li><code>IDLE</code>: memory and output keep their previous values unless reset changes <code>o_data</code>.</li> </ul> <p>This is a single-port BRAM, so one cycle performs one logical operation chosen by <code>i_op</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#reset-behavior","title":"Reset Behavior","text":"<p>Two reset-related details matter:</p> <ol> <li>The <code>initial</code> block fills the memory array with zeros. This helps simulation and also gives FPGA tools a zero-initialized BRAM image when supported.</li> <li>Runtime <code>i_rst</code> does not sweep through the whole memory and clear all rows. It only sets <code>o_data</code> to zero.</li> </ol> <p>Because of that, book_builder removes an order by explicitly writing an all-zero row back into the selected slot.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#addressing","title":"Addressing","text":"<p>This BRAM does not know anything about <code>order_ref_num</code> directly. book_builder computes the address first with <code>cal_order_book_addr()</code> and passes the folded result into <code>i_addr</code>.</p> <p>That means the BRAM itself is just a storage array; the hashing and collision risk live outside this module.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#synthesis-note","title":"Synthesis Note","text":"<p>The array is declared with:</p> <pre><code>(* ram_style = \"block\" *) reg [DATA_WIDTH-1:0] bram [0:(1&lt;&lt;ADDR_WIDTH)-1];\n</code></pre> <p>The attribute asks the synthesis tool to map this storage to block RAM instead of distributed LUT RAM when possible.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/bram/#limits","title":"Limits","text":"<ul> <li>There is no byte enable or partial update path; every write replaces the full row.</li> <li>There is no explicit valid-ready handshake on the output; the caller must already understand the one-cycle read latency.</li> <li>The module does not detect address collisions. If two order references hash to the same <code>i_addr</code>, the later write overwrites the earlier row.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/","title":"book_builder","text":"<p>This module is the per-symbol order-table stage inside symbol_book. It accepts parser events from order_book_parser, stores the current state of each live order, and emits compact quantity-delta messages for the bid-side and ask-side quantity books at the same time.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#structure","title":"Structure","text":"<p>Below gives the inside schematic of book builder:</p> <p></p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#design-logic","title":"Design Logic","text":"<ol> <li>fifo is necessary, as the operation cannot be done in one cycle.</li> <li>It maintains an order_book_inst inside, which is a bram saving the live order books. It is a database for the building of the price level books.</li> <li>When the incoming messages are being parsed, the state machine also translates the operations to qty_builder inside qty_book_wrapper to update the best bid/ask.</li> </ol>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code> is the main clock for the FIFO, state machine, and BRAM access.</li> <li><code>i_rst</code> clears the order table control state and the current quantity-delta output registers.</li> <li><code>i_stock_valid</code> gates events so only the configured symbol enters this <code>book_builder</code>.</li> <li><code>i_parser_msg</code> is the packed parser payload. In this design it carries <code>{msg_valid, seq_num, msg_type, stock_locate, order_ref_num, new_order_ref_num, side, shares, price}</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#outputs","title":"Outputs","text":"<ul> <li><code>o_qty_msg</code> is a <code>131-bit</code> packed quantity update <code>{bid_ask[1:0], price[31:0], is_add, d_shares[31:0], seq_num[63:0]}</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#order-table","title":"Order Table","text":"<p>Each table entry stores:</p> <ul> <li><code>valid</code>: whether the slot currently holds an order</li> <li><code>side</code>: <code>BID</code> (<code>2'b01</code>) or <code>ASK</code> (<code>2'b10</code>)</li> <li><code>shares</code>: current resting shares for that order</li> <li><code>price</code>: current resting price</li> </ul> <p>The address is not the raw <code>order_ref_num</code>. Instead, <code>cal_order_book_addr()</code> XOR-folds the 64-bit order reference into <code>BOOK_LEVEL_BIT</code> bits:</p> <pre><code>for (i = 0; i &lt; BOOK_LEVEL_BIT; i = i + 1) begin\n    for (j = 0; j &lt; 64; j = j + BOOK_LEVEL_BIT) begin\n        if (i + j &lt; 64) begin\n            cal_order_book_addr[i] = cal_order_book_addr[i] ^ order_ref_num[i+j];\n        end\n    end\nend\n</code></pre> <p>This keeps the table compact, but it also means different order references can collide and map to the same BRAM slot.</p> <p>The function can be modified if the entropy of the higher bits is low.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#state-machine","title":"State Machine","text":"<p>The update path uses four states because the BRAM has synchronous read latency.</p> <ul> <li><code>IDLE</code>: handle <code>A/F</code> immediately with one write, or start a read for <code>D/X/E/C/U</code>.</li> <li><code>FIRST_CYCLE</code>: keep the read request active while waiting for the stored order to appear on <code>book_o_data</code>.</li> <li><code>SECOND_CYCLE</code>: consume the stored order and perform delete, reduce, or the first phase of replace.</li> <li><code>THIRD_CYCLE</code>: finish the second write of a <code>U</code> replace event.</li> </ul> <p>In practice, add-order messages finish in one cycle, delete/reduce messages take three cycles, and replace messages take four cycles because they perform a remove followed by an add.</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#message-handling","title":"Message Handling","text":""},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#add-path","title":"Add path","text":"<ul> <li><code>TYPE_A</code> and <code>TYPE_F</code> write <code>{1'b1, msg_side, msg_shares, msg_price}</code> into the order table at <code>cal_order_book_addr(msg_order_ref_num)</code>.</li> <li>The same cycle, <code>o_qty_msg</code> reports an add event with <code>qty_is_add = 1'b1</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#remove-or-reduce-path","title":"Remove or reduce path","text":"<ul> <li><code>TYPE_D</code> reads the old order, clears the BRAM entry, and emits a remove event for the full resting shares.</li> <li><code>TYPE_X</code>, <code>TYPE_E</code>, and <code>TYPE_C</code> read the old order, subtract <code>msg_shares</code>, rewrite the order entry, and emit a remove event for <code>msg_shares</code>.</li> <li>If the order is missing, or if <code>book_o_shares &lt; msg_shares</code>, the module falls back to an idle output update.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#replace-path","title":"Replace path","text":"<ul> <li><code>TYPE_U</code> first reads and removes the old order referenced by <code>msg_order_ref_num</code>.</li> <li>One cycle later it writes a new entry under <code>msg_new_order_ref</code>.</li> <li>The new entry reuses the old side from <code>book_o_side</code>, together with the new shares and new price from the parser message.</li> <li>As a result, one <code>U</code> message generates two quantity updates on consecutive cycles: one remove, then one add.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#quantity-output","title":"Quantity Output","text":"<p>The quantity stream is the only public output of this block, and it is exactly what the downstream quantity books need:</p> <ul> <li><code>qty_bid_ask</code> selects bid or ask.</li> <li><code>qty_price</code> selects the price level to update.</li> <li><code>qty_is_add</code> distinguishes add from remove.</li> <li><code>qty_d_shares</code> is the delta applied at that level.</li> <li><code>qty_seq_num</code> carries the parser sequence number so downstream best-price snapshots can keep event ordering.</li> </ul> <p>When no valid update is being emitted, <code>qty_bid_ask</code> falls back to <code>IDLE</code> (<code>2'b00</code>).</p>"},{"location":"FPGA/order_book_builder/symbol_book/book_builder/overview/#design-limits","title":"Design Limits","text":"<ul> <li><code>msg_fifo_inst.o_push_ready</code> is not checked, so parser events can be dropped if the internal FIFO becomes full.</li> <li><code>cal_order_book_addr()</code> is a hash, not a collision-free order-reference mapping.</li> <li>The default parameter <code>PARSER_MSG_BIT = 1+64+8+16+64+64+2+32+32+48</code> still includes an extra <code>+48</code>, but the implemented field slices only use bits up to <code>i_parser_msg[282]</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/aligner/","title":"aligner","text":"<p>This module is the final alignment stage inside qty_book_wrapper. Its job is simple: make sure the reported best-valid and best-price signals line up with the BRAM-returned share count for that same best price level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/","title":"bram_dp","text":"<p>This module is the dual-port storage used inside qty_book_wrapper. It stores the live total shares at every tracked price level and allows the wrapper to update one price level while reading the shares of the current best price level at the same time.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#design-logic","title":"Design Logic","text":"<p>This storage is used because qty_book_wrapper needs two memory accesses in parallel:</p> <ol> <li>port A is used by qty_builder to read the old quantity of the updated price level and then write back the new total shares</li> <li>port B is used to read the shares stored at the current best price index selected by tree_builder</li> </ol> <p>Using a dual-port BRAM keeps the full quantity book in one memory block and avoids duplicating storage just to support those two access patterns.</p> <p>The tradeoff is the same as with ordinary FPGA BRAM: both reads and writes are clocked operations. A read result appears on <code>o_data_a</code> or <code>o_data_b</code> after the next rising edge, so aligner is needed to line up <code>best_valid</code> and <code>best_price</code> with the returned best-share data.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code>: BRAM clock.</li> <li><code>i_rst</code>: clears the two output registers.</li> <li><code>i_addr_a</code>: address for port A.</li> <li><code>i_op_a</code>: operation for port A, where <code>2'b01</code> is read and <code>2'b10</code> is write.</li> <li><code>i_data_a</code>: write data for port A.</li> <li><code>i_addr_b</code>: address for port B.</li> <li><code>i_op_b</code>: operation for port B, where <code>2'b01</code> is read and <code>2'b10</code> is write.</li> <li><code>i_data_b</code>: write data for port B.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#outputs","title":"Outputs","text":"<ul> <li><code>o_data_a</code>: registered read data from port A.</li> <li><code>o_data_b</code>: registered read data from port B.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#stored-format","title":"Stored Format","text":"<p>Unlike the order-table BRAM in book_builder/bram.md, this memory does not store full order state. Each row stores only the aggregated shares of one price level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#read-and-write-behavior","title":"Read And Write Behavior","text":"<p>Each port can independently request <code>IDLE</code>, <code>READ</code>, or <code>WRITE</code>:</p> <ul> <li><code>WRITE</code>: on the rising edge, the selected row is updated from the corresponding <code>i_data_*</code></li> <li><code>READ</code>: on the rising edge, the selected row is copied into <code>o_data_*</code></li> <li><code>IDLE</code>: the output for that port is driven to zero on the next clock if reset is not asserted</li> </ul> <p>So the module behaves like a synchronous dual-port BRAM with two independently controlled ports.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#reset-behavior","title":"Reset Behavior","text":"<p>Two reset-related details matter:</p> <ol> <li>The <code>initial</code> block fills the whole memory array with zeros.</li> <li>Runtime <code>i_rst</code> only clears <code>o_data_a</code> and <code>o_data_b</code>; it does not walk through the entire memory and erase every row.</li> </ol> <p>So if the design wants to clear one price level during normal operation, it must explicitly write zero into that row.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/bram_dp/#limits","title":"Limits","text":"<ul> <li>There is no byte enable or partial write path.</li> <li>The outputs are registered, so users of this module must account for read latency explicitly.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/","title":"qty_book_wrapper","text":"<p>This module maintains the price-level quantity book for one side of one symbol. It receives compact quantity-delta events from book_builder, updates the total shares at the affected price level, tracks which price level is currently the best, and outputs the best price with its corresponding shares.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#structure","title":"Structure","text":"<p>Below privides the structure inside <code>qty_book_wrapper</code> module.</p> <p></p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#introduction","title":"Introduction","text":"<p>Inside symbol_book, there are two instances of this wrapper:</p> <ol> <li>one ask-side wrapper</li> <li>one bid-side wrapper</li> </ol> <p>Both instances consume the same <code>i_qty_msg</code> stream, but each wrapper only reacts to messages for its configured side.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#design-logic","title":"Design Logic","text":"<p>The wrapper splits the problem into four smaller blocks:</p> <ol> <li>qty_builder: updates the shares stored at one price level and reports when that level changes between empty and non-empty.</li> <li>tree_builder: tracks which valid price index is currently the best for this side.</li> <li>aligner: delays the best-valid and best-price signals so they line up with the BRAM read data for best shares.</li> <li>a dual-port BRAM: stores the live quantity at every tracked price level.</li> </ol> <p>This split is useful because the design needs to answer two different questions at the same time:</p> <ul> <li>What is the new total quantity at the updated price level?</li> <li>After the update, which price level is now the best?</li> </ul> <p>The double-port BRAM keeps the full quantity book, while the tree keeps only the occupancy information needed to find the best index quickly.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code>: main clock for the wrapper.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_qty_msg</code>: packed quantity update from book_builder. In <code>symbol_book</code>, the format is <code>{bid_ask, price, is_add, d_shares, seq_num}</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#outputs","title":"Outputs","text":"<ul> <li><code>o_best_valid_aligned</code>: high when the wrapper has a valid best price and aligned best shares.</li> <li><code>o_best_price_aligned</code>: best price for this side.</li> <li><code>o_best_shares</code>: total resting shares at the best price.</li> <li><code>o_seq_num</code>: sequence number attached to the best-price event. When no valid best exists, the output is forced to zero.</li> <li></li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#data-path","title":"Data Path","text":"<p>The wrapper operates like this:</p> <ol> <li>qty_builder filters <code>i_qty_msg</code> by side.</li> <li>It converts the absolute <code>price</code> into a compact price index.</li> <li>It reads the current quantity from BRAM_dp port A, computes the new quantity, and writes the updated value back.</li> <li>If one price level changes from <code>0 -&gt; non-zero</code> or <code>non-zero -&gt; 0</code>, it sends that change to tree_builder.</li> <li>tree_builder updates the best valid price index for the current side.</li> <li>The wrapper uses BRAM_dp port B to read the shares stored at that best index.</li> <li>aligner delays <code>best_valid</code> and <code>best_price</code> so they arrive in the same cycle as the BRAM read data for <code>o_best_shares</code>.</li> </ol> <p>Because the best-price shares come from BRAM_dp, the output path is not purely combinational. The aligner is what makes the three outputs correspond to the same best level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#price-level-book","title":"Price-Level Book","text":"<p>The quantity book is not keyed by order reference. It is keyed by price level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#best-price-policy","title":"Best Price Policy","text":"<p>tree_builder stores valid information for all tracked price levels and returns one best index:</p> <ul> <li>for the bid side, the higher valid price index wins</li> <li>for the ask side, the lower valid price index wins</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#why-dual-port-bram-is-used","title":"Why Dual-Port BRAM Is Used","text":"<p>The wrapper needs two BRAM accesses that conceptually happen in parallel:</p> <ol> <li>port A updates the quantity of the price level touched by the newest delta</li> <li>port B reads the quantity stored at the current best price level</li> </ol> <p>Using a dual-port BRAM allows the design to keep the live quantity book in one memory while still producing <code>o_best_shares</code> for the current best level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#latency","title":"Latency","text":"<p>The wrapper is pipelined:</p> <ul> <li>qty_builder uses a read-modify-write sequence for each quantity update</li> <li>tree_builder contains a registered mid-level stage</li> <li>aligner adds delay so price/valid line up with best-share BRAM data</li> </ul> <p>As a result, the best-price output is not available in the same cycle as the input quantity message.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/overview/#limits","title":"Limits","text":"<ul> <li>The tree tracks whether a price level is empty or non-empty; it does not store the exact quantity itself.</li> <li>The internal FIFOs do not expose backpressure handling at the wrapper boundary.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/","title":"qty_builder","text":"<p>This module is the update engine inside qty_book_wrapper. It receives quantity-delta messages for both sides, keeps only the messages for its configured side, updates the total shares stored at the affected price level, and tells tree_builder when that price level changes between empty and non-empty.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#design-logic","title":"Design Logic","text":"<p>This module itself does not contain a bram, it controls a double-port bram outside it, which is shared with tree_builder. Thus, the output of this module is the address, operation and data for the bram. Also, it tells tree_builder every time when the shares of a price becomes zero or from zero becomes non-empty. This signal triggers tree_builder to find the new best price.</p> <p>Because the operation cannot be done in one cycle or in pipeline, there is a fifo inside.</p> <p>If the fifo is not empty and the state machine is in <code>IDLE</code>, it pops out the data and latch it for operation (the latch may be improved in the future design as the output of the fifo itself is already latched if there is no new pop signal). Then, it starts the state machine for live price level update and message sending to tree_builder.</p> <p>The operation is a fixed routine. First, read the data from the double-port bram, the address is the latched price index. You do not know whether there is already a share in the live price book, so you cannot skip this step. The second is to wait for the read to be finished. The third is write back the new shares of that price. The new shares have already been prepared once the read is finished. The calculation is easy, just add or minus from the read-out data with code give below:</p> <pre><code>qty_new = is_add ? (qty_cur + d_shares)\n                 : ((qty_cur &gt; d_shares) ? (qty_cur - d_shares) : 0)\n</code></pre> <p>At the same time, there is a combinational logic watching if the state machine is in the <code>SECOND_CYCLE</code>, which is the write cycle. Because at this step, we have the current shares and the new shares for the price. This is directly used to check whether the shares become to zero or from zero to non-empty. We send out this event signal to tree_builder to find the new best price.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk_156</code>: main clock.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_qty_msg</code>: packed quantity update. In the wrapper integration, the format is <code>{bid_ask, price, is_add, d_shares, seq_num}</code>.</li> <li><code>i_bram_o_data</code>: current quantity read back from BRAM port A.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#outputs","title":"Outputs","text":"<ul> <li><code>o_tree_price_idx</code>: price index whose occupancy changed.</li> <li><code>o_tree_price_change</code>: <code>NON_EMPTY</code>, <code>EMPTY</code>, or <code>IDLE</code>.</li> <li><code>o_seq_num</code>: sequence number latched from the current quantity message.</li> <li><code>o_bram_addr</code>: BRAM address for the current price level.</li> <li><code>o_bram_op</code>: BRAM operation, where <code>READ</code> is <code>2'b01</code> and <code>WRITE</code> is <code>2'b10</code>.</li> <li><code>o_bram_i_data</code>: new total shares written back to BRAM.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#message-filtering","title":"Message Filtering","text":"<p>The wrapper sends the same <code>i_qty_msg</code> stream to both bid-side and ask-side instances. <code>qty_builder</code> accepts only the messages whose side matches <code>BID_OR_ASK</code>:</p> <pre><code>wire ff_push = i_qty_side == BID_OR_ASK;\n</code></pre> <p>So one instance updates only bid levels, and the other updates only ask levels.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#price-mapping","title":"Price Mapping","text":"<p>The module does not use the raw price as a BRAM address. It converts <code>price</code> into a compact index with <code>cal_qty_book_addr()</code>:</p> <pre><code>price_idx = (price - PRICE_BASE) &gt;&gt; 2\n</code></pre> <p>This means adjacent tracked price levels are spaced by <code>4</code> in the encoded price unit.</p> <p>Example with <code>PRICE_BASE = 100</code>:</p> <pre><code>price = 112\nprice_idx = (112 - 100) &gt;&gt; 2 = 3\n</code></pre> <p>If the computed offset is negative or beyond the configured depth, the function returns index <code>0</code>.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#tree-update-pulse","title":"Tree Update Pulse","text":"<p><code>qty_builder</code> does not notify tree_builder on every quantity change. It only notifies the tree when the price level changes occupancy:</p> <ul> <li><code>0 -&gt; non-zero</code>: emit <code>NON_EMPTY</code></li> <li><code>non-zero -&gt; 0</code>: emit <code>EMPTY</code></li> <li>otherwise: emit <code>IDLE</code></li> </ul> <p>This is enough because the tree only needs to know whether a price level should participate in best-price selection.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#bram-interface","title":"BRAM Interface","text":"<p>This module drives only one side of bram_dp:</p> <ul> <li><code>o_bram_addr</code>: selected price index</li> <li><code>o_bram_op</code>: <code>READ</code> during the first phase, then <code>WRITE</code></li> <li><code>o_bram_i_data</code>: new total shares during the write phase</li> </ul> <p>The stored BRAM value is just the aggregated shares for that price level, not the raw order data.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/qty_builder/#limits","title":"Limits","text":"<ul> <li>Prices below <code>PRICE_BASE</code> or outside the tracked depth collapse to index <code>0</code>.</li> <li>The FIFO push-ready signal is not checked here, so sustained overflow would need system-level handling.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/","title":"tree_builder","text":"<p>Its job is to track which price levels are active and return the best valid price index for one side of the quantity book. This is one of the final outputs from order_book_builder</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#introduction","title":"Introduction","text":"<p>This block is used inside qty_book_wrapper after qty_builder updates one price level. It does not calculate share totals. It only tracks whether each price index is empty or non-empty, then reduces that information into one best price index.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#design-logic","title":"Design Logic","text":"<p>qty_book_wrapper splits the problem into two parts:</p> <ol> <li>qty_builder updates the shares stored at one price level in the double-port bram and reports whether that level changed occupancy.</li> <li>this module updates the best price index based on those occupancy changes</li> <li>The corresponding shares then be read from double-port bram. This is the tree_builder side operation.</li> </ol> <p>That split is useful because best-price selection does not require the exact share count of every level. It only requires knowing which price indices are currently valid.</p> <p>The internal structure is a reduction tree (refer to endrias_Project_Final_Report.pdf):</p> <p>I have redrawn it to a four-level structure for better explanation, as give below:</p> <p></p> <p>The bottom registers are one-on-one to all the valid price index. Thus, the left is always less than right. Once a price index moves from empty to non-empty or vice versa, the bottom registers will also change accordingly. this is in </p> <pre><code>if (i_price_change != IDLE) begin\n    btm_valid[i_price_idx] &lt;= (i_price_change == NON_EMPTY);\nend\n</code></pre> <p>The layer uppon bottom will automatically update values in each node,  The winner rule depends on <code>BID_OR_ASK</code>:</p> <ul> <li>bid-style selection chooses the higher valid index</li> <li>ask-style selection chooses the lower valid index</li> </ul> <p>This can be noticed in the following block.</p> <pre><code>if (BID_OR_ASK == 2'b01) begin\n   // For bid tree, if both children are valid, take the right one (higher price).\n   assign mid_to_btm_idx[level][node] =\n       btm_valid[RIGHT_CHILD] ? RIGHT_LEAF_IDX :\n       btm_valid[LEFT_CHILD]  ? LEFT_LEAF_IDX  :\n                                       {QTY_PRICE_LVL_BIT{1'b0}};\nend else if (BID_OR_ASK == 2'b10) begin\n   // For ask tree, if both children are valid, take the left one (lower price).\n   assign mid_to_btm_idx[level][node] =\n       btm_valid[LEFT_CHILD]  ? LEFT_LEAF_IDX  :\n       btm_valid[RIGHT_CHILD] ? RIGHT_LEAF_IDX :\n                                       {QTY_PRICE_LVL_BIT{1'b0}};\nend\n</code></pre> <p>This update will propagate.</p> <p>Instead of using an all-register tree, which only updates one level per cycle, here the combinational and sequential logics are combined to construct the tree. Only the <code>bottom</code>, <code>mid</code> and the <code>top</code> nodes are registers while other nodes are all wires. this makes the update faster.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#interface","title":"Interface","text":""},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#inputs","title":"Inputs","text":"<ul> <li><code>i_clk</code>: main clock.</li> <li><code>i_rst</code>: active-high reset.</li> <li><code>i_price_idx</code>: price index whose occupancy changed.</li> <li><code>i_price_change</code>: <code>NON_EMPTY</code>, <code>EMPTY</code>, or <code>IDLE</code>.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#outputs","title":"Outputs","text":"<ul> <li><code>o_best_valid</code>: high when at least one tracked price level is non-empty.</li> <li><code>o_best_price_idx</code>: best valid price index for this side.</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#occupancy-input","title":"Occupancy Input","text":"<p>The input event comes from qty_builder:</p> <ul> <li><code>NON_EMPTY</code>: the price level changed from <code>0 -&gt; non-zero</code></li> <li><code>EMPTY</code>: the price level changed from <code>non-zero -&gt; 0</code></li> <li><code>IDLE</code>: no occupancy change</li> </ul>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#output-meaning","title":"Output Meaning","text":"<p><code>o_best_valid</code> tells downstream logic whether any price level is currently active.</p> <p><code>o_best_price_idx</code> is only meaningful when <code>o_best_valid = 1</code>. qty_book_wrapper converts that index back into a price and uses bram_dp port B to read the shares stored at that best level.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#complexity","title":"Complexity","text":"<p>With <code>QTY_PRICE_LVL_BIT = N</code>, the tree covers <code>2^N</code> price levels.</p>"},{"location":"FPGA/order_book_builder/symbol_book/qty_book_wrapper/tree_builder/#limits","title":"Limits","text":""}]}
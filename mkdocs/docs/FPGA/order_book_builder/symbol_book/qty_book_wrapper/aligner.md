# aligner
This module is the final alignment stage inside [qty_book_wrapper](overview.md). Its job is simple: make sure the reported best-valid and best-price signals line up with the BRAM-returned share count for that same best price level.

